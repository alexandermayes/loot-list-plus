---
phase: 02-checkable-conversion-copy
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md
autonomous: false
requirements: [COPY-01, PROOF-01, PROOF-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "One artifact holds every user-facing string this phase will change, each with its file path, its exact current value, and its proposed replacement value (D-16)"
    - "The signup draft covers all five COPY-01 elements: page title, H1, body, primary CTA label, and secondary link text, each starting from the sprint plan's wording and adapted to the shipped hero voice (D-07)"
    - "The testimonial draft renders one concrete verification line per quote, built only from data recorded in 02-COPY-AUDIT.md, with no field the user marked NOT SUPPLIED (D-02, D-03, D-04)"
    - "The repositioning draft assigns every occurrence in the audit's inventory a disposition of replace with a named replacement string, or keep with a reason (D-12)"
    - "A per-file line of the exact form `RESIDUAL-ALLOWANCE: <path> = <N>` records how many occurrences of the narrower positioning phrase are approved to remain in each swept file, so the shipping plans can gate on a number instead of on a blanket zero"
    - "A checklist of externally needed wording changes exists for the founder-owned surfaces the executor must not edit: the Discord server description, the GitHub repository description field, and any old external posts (D-14)"
    - "No em dash appears in any proposed replacement string in the artifact"
    - "The artifact carries a `SIGN-OFF: APPROVED <ISO date>` line only after the user has approved, and every approved string is recorded verbatim under an `### Approved` heading"
    - "Every changed string is recorded once as a line of the exact form `APPROVED-STRING: <key> = <exact final string>`, so each shipping plan can prove its committed source matches what the user approved byte for byte"
    - "Every open question research flagged is presented as a named choice at the one sign-off pass rather than defaulted silently: the blog ranking-title scope, the four undecided superlatives, and the /compare CTA destination defect"
  artifacts:
    - path: ".planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md"
      provides: "Every proposed string for this phase, the repositioning disposition table, the residual allowances, the external handoff checklist, and the recorded sign-off"
      contains: "SIGN-OFF:"
  key_links:
    - from: ".planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md"
      to: "app/components/LoginPage.tsx"
      via: "plan 02-03 copies approved strings verbatim out of this artifact into the signup surface"
      pattern: "SIGN-OFF: APPROVED"
    - from: ".planning/phases/02-checkable-conversion-copy/02-COPY-AUDIT.md"
      to: ".planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md"
      via: "every audit finding and every inventory row must appear in the draft with a disposition"
      pattern: "## Repositioning Inventory"
  prohibitions:
    - requirement_id: COPY-01
      category: transparency
      status: unverified
      flagged: true
      statement: "Signup copy MUST NOT assert a time-to-value or time-saved claim (for example set up in minutes, saves hours a week) that no measurement in this repo or on the site supports. The sprint plan's own starting-point H1 makes exactly this kind of claim, and this phase exists to remove unverifiable claims."
    - requirement_id: COPY-01
      category: values
      status: unverified
      flagged: true
      statement: "Signup copy MUST NOT use urgency, scarcity, shaming, or loss-aversion framing to drive signup (for example implying a guild is losing loot or falling behind by not signing up)."
    - requirement_id: COPY-01
      category: transparency
      status: unverified
      flagged: true
      statement: "No signup string may imply Retail or current-expansion support, which does not exist today (D-13). Broad wording naming World of Warcraft is acceptable because Classic is World of Warcraft; a compatibility claim is not."
    - requirement_id: PROOF-01
      category: transparency
      status: unverified
      flagged: true
      statement: "The verification line MUST NOT present a self-attested note as independent third-party verification, and MUST NOT imply that Warcraft Logs endorses or has audited LootList+ merely because a guild's public profile is linked."
    - requirement_id: PROOF-01
      category: privacy
      status: unverified
      flagged: true
      statement: "MUST NOT publish per-quote identifying detail (role, guild, expansion or tier, interview date) or an outbound public profile link beyond what that quote's author consented to. Consent to the quote wording is not consent to new attribution fields or to an outbound link."
    - requirement_id: PROOF-01
      category: transparency
      status: unverified
      flagged: true
      statement: "A quote from a guild that is no longer active MUST NOT read as a current-activity claim. The date-anchored variant exists so the page never implies a historical quote comes from a currently active customer."
    - requirement_id: PROOF-02
      category: transparency
      status: unverified
      flagged: true
      statement: "A removed unverifiable claim MUST NOT be replaced by another unverifiable claim or by a superlative. Every replacement must be confirmable by a visitor from content on the site or from a public source linked on the page."
    - requirement_id: PROOF-02
      category: transparency
      status: unverified
      flagged: true
      statement: "An audit-identified unverifiable claim MUST NOT be silently left standing. Each one ends as replaced, removed, or explicitly accepted by the user with a recorded reason."
  flagged_assumptions:
    - "PLANNER ASSUMPTION (D-16 ordering): CONTEXT.md D-16 requires one consolidated sign-off and states that nothing user-facing ships before it. `.planning/config.json` sets `git.branching_strategy: none`, so execution commits land on main, and this repo auto-deploys from main. The planner therefore treats commit, not merge, as the shipping event, which forces a draft-then-approve-then-apply order across waves rather than a per-surface vertical slice. This overrides the default tracer-first ordering for waves 1 and 2. Flagged, not resolved. If the developer intends to execute this phase on a branch, the ordering constraint relaxes and waves 3 and 4 could start earlier."
    - "PLANNER ASSUMPTION (D-07 voice): the established hero voice is taken from the shipped H1 `Epic loot deserves an epic system.` and the shipped hero body in `app/components/landing/LandingHero.tsx`, since no separate voice guide exists in the repo. Flagged, not resolved."
---

<objective>
Draft every string this phase will change into one artifact, then get one consolidated human approval on all of it.

Purpose: CONTEXT.md D-16 locks this phase behind a single sign-off pass, and CLAUDE.md makes user sign-off a hard gate on user-facing copy. Drafting everything in one place is what makes a single pass possible: the user sees the signup copy, the testimonial verification lines, the stats replacement, the titles and metas, and the repositioning wording together, and can judge them as one voice rather than approving five disconnected diffs.

Output: `.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md`, carrying every proposed string, every repositioning disposition, the external handoff checklist, and the recorded approval that plans 02-03 through 02-07 gate on.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/loot-list-plus/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/loot-list-plus/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-checkable-conversion-copy/02-CONTEXT.md
@.planning/phases/02-checkable-conversion-copy/02-UI-SPEC.md
@.planning/phases/02-checkable-conversion-copy/02-01-SUMMARY.md
@.planning/phases/02-checkable-conversion-copy/02-COPY-AUDIT.md
</context>

<assumption_delta_decision>
**Noun that is now primary:** the positioning noun for the product's game scope.

**Delta detected:** D-12 broadens the positioning wording from the specific "WoW Classic" to the general "World of Warcraft". A specific term has lost its monopoly on the primary slot.

**Decision: promote.** "World of Warcraft" becomes the primary positioning noun on every in-repo user-facing surface, and the narrower "Classic" wording is demoted to a detail of one variant: the checkable, feature-level expansion facts ("5 supported Classic expansions", the expansion list on /about, the range on /pricing). This is not add-alongside. A surface that keeps the narrow term in its primary positioning slot while a sibling surface broadens is the exact half-applied state D-12 calls out as worse than either consistent state.

**Rationale:** D-13 makes the promotion safe. Broad positioning wording is truthful because Classic is World of Warcraft, while feature-level compatibility claims stay expansion-specific and checkable, so promoting the general noun does not smuggle in a Retail-support claim.

**Accepted exception, not debt:** search keywords and shorthand are a deliberate add-alongside. Per the user's added note in CONTEXT.md, "WoW" stays present next to "World of Warcraft" where natural, and the `keywords` array in `app/layout.tsx` is expected to keep both forms because both are real search terms. That exception is recorded per occurrence in the disposition table, never applied by find-and-replace.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Draft every string, every repositioning disposition, the residual allowances, and the external handoff checklist into one artifact</name>
  <precondition>`.planning/phases/02-checkable-conversion-copy/02-COPY-AUDIT.md` exists and contains a `## Quote Metadata (user supplied)` section (the plan 02-01 checkpoint completed).</precondition>
  <reversibility rating="costly">D-12's repositioning touches 13 files and the standing full-sweep rule, and D-03's verification-line shape becomes the proof pattern Phases 3 and 4 echo. Both were chosen by the user in CONTEXT.md, so no gate is re-asked here; the rating is recorded so a later phase sees the weight.</reversibility>
  <files>.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md</files>
  <read_first>
    - `.planning/phases/02-checkable-conversion-copy/02-COPY-AUDIT.md` (all five sections)
    - `/Users/alexander.mayes/Downloads/LootList_30_Day_Search_AI_Sprint.md`, sections "Signup page", "Testimonial display format", "Replace the unsupported stats block", "Exact positioning and site copy". These are the exact starting-point copy sources named in CONTEXT.md canonical_refs.
    - `app/components/landing/LandingHero.tsx` (the shipped hero voice this draft must match)
    - `app/components/LoginPage.tsx` lines 140 to 180 (current signup copy)
    - `app/page.tsx` lines 9 to 19 (current signup metadata, including the intentional noindex)
    - `app/(landing)/landing/page.tsx`, `app/changelog/layout.tsx`, `app/compare/page.tsx`, `app/layout.tsx` (the four metadata blocks D-09, D-10, D-11, and the D-12 root touch)
    - `scripts/analytics/exports/gsc-trend-query-2026-05-24_2026-08-23.csv` and `scripts/analytics/ai-answer-log.csv` (the evidence behind D-09, D-10, D-11)
    - `.planning/phases/02-checkable-conversion-copy/02-UI-SPEC.md`, section "Copywriting Contract"
  </read_first>
  <action>
    Create `.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md`. Every proposed string in it is a draft awaiting the Task 2 sign-off. Mark the whole artifact `STATUS: DRAFT, PENDING SIGN-OFF` on its first line. Write no source file in this task.

    Section `## A. Signup copy (COPY-01)`. Five rows, one per element, with columns `element`, `current shipped string`, `sprint plan starting point`, `proposed draft`, `rationale`. The current shipped strings are: H1 `Epic loot deserves an epic system.` at `app/components/LoginPage.tsx:145`, body `LootList+ is a transparent loot management system for WoW guilds. Includes loot submissions, attendance tracking and more.`, primary CTA `Sign up with Discord` at line 170, secondary link text `See how it works` at line 177, and page title `LootList+ ∙ Sign up` in `app/page.tsx`. The sprint plan starting points, per D-07 and the UI-SPEC Copywriting Contract, are H1 `Set up fair loot in minutes.`, CTA `Continue with Discord`, and secondary link `See how LootList+ works`. Adapt each into the shipped hero voice (concise, personality first) and give at least two candidate options per element so the user has something to choose between rather than only something to approve. Every candidate must clear the prohibitions recorded in this plan's frontmatter, and the draft must state, per candidate, that it does. In particular, flag the plan's own starting-point H1 explicitly: it asserts a setup speed that nothing in this repo measures, which is the precise class of claim this phase exists to remove, so at least one candidate must carry no time claim at all. Also note that the H1 here is the signup surface in `app/components/LoginPage.tsx`, not the marketing hero H1 in `LandingHero.tsx`, which D-10 protects and this phase never touches. Record the D-08 destination as fixed, not drafted: the secondary link href becomes the absolute cross-domain URL `https://www.getlootlist.com/#how-it-works`, because the signup surface renders on lootlistplus.com and the target section id lives at `app/components/landing/LandingHowItWorks.tsx:40` on the marketing domain. Record that `robots: { index: false }` in `app/page.tsx` stays as it is; D-09, D-10, and D-11 do not apply to a login surface.

    Section `## B. Testimonial verification lines (PROOF-01)`. One block per QuoteCard call site found in the audit. For each: the guild as written in the JSX, the author name, every user-supplied metadata field verbatim, every unsupplied field marked `NOT SUPPLIED`, the D-03 and D-04 variant chosen for that guild, and the exact rendered line text. Build the line on the sprint plan's verbatim shape recorded in CONTEXT.md: the verified-customer note, then the interview month and year, then the guild or Warcraft Logs profile, separated by a middle dot. A quote with an unsupplied interview date cannot carry the dated variant; say so in the block rather than substituting a nearby date. A quote whose author did not consent to an outbound link gets the text-only note even if the guild has a Warcraft Logs URL on file. Per D-01 no quote wording changes and no quote is removed. Per D-05 no structured data is proposed for this section; state that explicitly so a later reader does not add it back.

    Section `## C. Stats block (PROOF-02)`. Per D-06 the only card whose value and label change is the Row 2 card at `app/components/landing/LandingValueProps.tsx:143`, currently rendered with `value="3+"`. Its replacement is the fact recorded in the audit's Expansion Fact Check section, drafted as a `value` and `label` pair in the same shape the sibling cards use. State the two sibling cards that stay untouched: the one whose value is `0` and the one whose value is `1`. Then list, as named sign-off questions and not as applied decisions, every other unverifiable claim the audit found: the `100%` / `transparent` card at line 128, the superlative at `app/components/landing/LandingCompare.tsx:91`, and the `The ultimate loot management system` phrasing in `public/site.webmanifest:4` and in the openGraph and twitter descriptions of `app/(landing)/landing/page.tsx`. For each, propose a replacement and let the user choose fix, remove, or keep with a reason.

    Section `## D. Titles and metadata (D-09, D-10, D-11)`. Three blocks. For each page, treat `title`, `description`, `openGraph.title`, `openGraph.description`, `twitter.title`, `twitter.description`, and where present `jsonLd.headline` and `jsonLd.description` as one copy unit and draft all present siblings together; the homepage file already carries drift between its plain description and its more superlative openGraph description, and this draft must close it rather than reproduce it. Homepage (`app/(landing)/landing/page.tsx`): a category-forward title leading with the words officers actually search (loot list, attendance, Loot Score) with the brand attached, per D-10. Changelog (`app/changelog/layout.tsx`): an unambiguously changelog-intent title and description, per D-09, and no noindex. Cite the cannibalization evidence in the block: /changelog currently takes 514 of 597 impressions on the top generic query at 0.19 percent CTR. Compare (`app/compare/page.tsx`): a title and description built from checkable claims rather than marketing superlatives, aimed at competitor queries, per D-11. Cite the evidence: the week-1 AI answer log shows Claude citing /compare in 3 of 6 answers while flagging it as vendor-written marketing, and GSC shows position roughly 9 to 12.6 at 0 to 1.31 percent CTR on those queries. Note that `app/compare/page.tsx` holds three separate copies of description-style text (`metadata.description`, `metadata.openGraph.description`, `jsonLd.description`) that must move together.

    Section `## E. Repositioning dispositions (D-12)`. Copy every row of the audit's Repositioning Inventory forward and fill in the proposed replacement string and the disposition. Then, immediately after the table, emit one line per swept file of exactly this form, with no leading whitespace:

    `RESIDUAL-ALLOWANCE: <path> = <N>`

    where `<path>` is the repository-relative path and `<N>` is the count of occurrences approved to remain in that file after the sweep. Emit one such line for each of the 13 files in the inventory, including files whose approved count is 0. These lines are the numeric gate the shipping plans check against, so the format must be exact.

    Section `## F. External handoff checklist (D-14)`. A checklist the user executes on founder-owned surfaces the executor must not touch: the Discord server description, the GitHub repository description field (distinct from the in-repo `README.md`, which this phase does edit), and any old external posts. For each, give the current wording if known, the proposed wording, and where to change it. State plainly that no executor edits these.

    Section `## G. Open questions for sign-off`. Numbered, each with a recommendation and the cost of each answer. At minimum: (1) the six occurrences in `app/blog/how-to-run-loot-without-a-spreadsheet/page.tsx`, three of which are the indexed title, the on-page H1, and the JSON-LD headline, where changing a ranking title carries more search volatility than changing a meta description; (2) each undecided superlative from section C; (3) the `app/compare/page.tsx:32` CTA destination defect, where `APP_URL` points at the marketing domain while every sibling file points at the signup domain, which is out of D-11's scope but sits in a file the executor is already editing; (4) the activity window used for D-04 and any guild the lookup could not resolve; (5) whether the quote count is four or five.

    Finally, before finishing, grep your own artifact for the em dash character and remove every one from every proposed string. There is no lint rule for this in the repo; PR #253 was a manual sweep.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md && for s in '^## A\. Signup copy' '^## B\. Testimonial verification lines' '^## C\. Stats block' '^## D\. Titles and metadata' '^## E\. Repositioning dispositions' '^## F\. External handoff checklist' '^## G\. Open questions for sign-off'; do grep -qE "$s" .planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md || { echo "missing section: $s"; exit 1; }; done; N=$(grep -cE '^RESIDUAL-ALLOWANCE: [^ ]+ = [0-9]+$' .planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md); test "$N" -ge 13 || { echo "expected at least 13 RESIDUAL-ALLOWANCE lines, found $N"; exit 1; }; grep -q 'STATUS: DRAFT, PENDING SIGN-OFF' .planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md && ! grep -q '—' .planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md && test -z "$(git status --porcelain -- app public README.md)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md` exists and contains all seven section headings A through G.
    - Section A contains a row for each of the five COPY-01 elements and at least two candidate drafts per element, and at least one H1 candidate contains no time-to-value claim.
    - Section A records the secondary link destination as the literal `https://www.getlootlist.com/#how-it-works` and records that `robots.index` stays false on `app/page.tsx`.
    - Section B contains one block per QuoteCard call site listed in the audit, and every metadata field is either a verbatim user-supplied value or the literal `NOT SUPPLIED`.
    - Section E's disposition table has the same number of occurrence rows as the audit's Repositioning Inventory, and every `keep` row carries a reason.
    - At least 13 lines match `^RESIDUAL-ALLOWANCE: [^ ]+ = [0-9]+$`, one per file in the inventory.
    - `grep -c '—' .planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md` returns 0.
    - `git status --porcelain -- app public README.md` is empty. No shipping surface is touched by this task.
  </acceptance_criteria>
  <done>Every string this phase will change exists as a labelled draft in one artifact, with residual allowances emitted and open questions named, and no source file has changed.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: The one consolidated copy sign-off (D-16)</name>
  <files>.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md</files>
  <decision>Approve the final wording of every user-facing string this phase will ship, and answer the five open questions</decision>
  <context>
    This is the one consolidated copy sign-off (D-16). Nothing user-facing has been committed yet and nothing will be until you approve, because this repo commits to main and main auto-deploys to production. `02-COPY-DRAFT.md` holds every proposed string in one place so you can judge them as one voice: the signup copy, the testimonial verification lines, the stats replacement, the homepage, changelog, and compare titles and metas, and the repositioned positioning wording across 13 files.

    Three things worth your attention before you read the drafts.

    The sprint plan's own signup H1 asserts a setup speed that nothing in this repo measures. That is the same class of claim this phase exists to delete from the homepage, so section A offers at least one candidate with no time claim in it.

    The testimonial verification lines add attribution fields and, for some quotes, an outbound link to a guild's public Warcraft Logs page. The quote authors consented to their wording, not necessarily to the new fields or to being pointed at publicly. Section B records what you told me about consent per quote; confirm it reads right.

    The repositioning sweep is all or nothing. A half-applied broadening, where the marketing pages say one thing and the site-wide JSON-LD still says another, is worse than either consistent state. Section E lists every occurrence and what happens to it, including the ones that deliberately stay.
  </context>
  <options>
    <option id="approve-all">
      <name>Approve every drafted string as written</name>
      <pros>Plans 02-03 through 02-07 can start immediately and ship exactly what you read.</pros>
      <cons>Only correct if you are happy with every candidate chosen in the draft, including the answers recommended in section G.</cons>
    </option>
    <option id="approve-with-edits">
      <name>Approve with edits</name>
      <pros>You rewrite or pick per string. The executor records your exact wording verbatim as the approved value and ships that, byte for byte.</pros>
      <cons>Requires you to go element by element through sections A to E.</cons>
    </option>
    <option id="revise-and-represent">
      <name>Send it back for another drafting pass</name>
      <pros>Right answer if the voice is off across the board rather than in specific strings.</pros>
      <cons>Costs another full drafting cycle before any surface can ship.</cons>
    </option>
  </options>
  <resume-signal>
    Reply with your choice, your per-string edits if any, and your answers to the five open questions in section G: the blog ranking-title scope, each undecided superlative, the /compare CTA destination defect, the D-04 activity window and any unresolved guild, and whether the quote count is four or five.

    On approval the executor rewrites `02-COPY-DRAFT.md` so that each section carries an `### Approved` subsection holding the final strings verbatim, replaces the first line with `STATUS: APPROVED`, appends a line of the exact form `SIGN-OFF: APPROVED <ISO date>`, and updates the `RESIDUAL-ALLOWANCE` lines to match your answers.

    It also emits, inside the `### Approved` subsections, one line per changed string of exactly this form, with no leading whitespace:

    `APPROVED-STRING: <key> = <exact final string>`

    Keys are stable and lower-kebab, scoped by surface: `signup.title`, `signup.h1`, `signup.body`, `signup.cta`, `signup.secondary-text`, `signup.secondary-href`, `stat.row2.value`, `stat.row2.label`, `quote.<guild-slug>.verification`, `meta.<page>.title`, `meta.<page>.description`, `meta.<page>.og-title`, `meta.<page>.og-description`, `meta.<page>.twitter-title`, `meta.<page>.twitter-description`, `meta.<page>.jsonld-headline`, `meta.<page>.jsonld-description`, and `repos.<path>.<line>` for each repositioning replacement. These lines are the parity gate every shipping plan checks its committed source against, so the format must be exact and the value must be the final string byte for byte, unescaped and unquoted.

    Plans 02-03 through 02-07 halt unless the `SIGN-OFF: APPROVED` line is present.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drafted copy to the public website | Every string approved here becomes a public claim. An unverifiable claim that survives this gate ships. |
| planning artifact to the git repository | The draft artifact is committed to a public repo, so anything recorded here about a customer is public. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Repudiation | Testimonial attribution drafted in section B | high | mitigate | Every metadata field is either a verbatim user-supplied value or the literal `NOT SUPPLIED`. The executor invents nothing (D-02). Consent to an outbound public profile link is recorded per quote and the text-only variant is used when consent is absent. |
| T-02-07 | Spoofing | A verification line that reads as third-party attestation when it is self-attested | high | mitigate | Prohibition recorded in this plan's frontmatter. The line names LootList+ as the attester and a linked Warcraft Logs profile is presented as the guild's own public page, never as an endorsement. Judgment-tier, reviewed by the human at this plan's sign-off. |
| T-02-08 | Tampering | An approved string containing angle brackets or a script-closing sequence injected into a JSON-LD block rendered with dangerouslySetInnerHTML in `app/layout.tsx` and `app/compare/page.tsx` | low | mitigate | Approved strings for any JSON-LD field are restricted to plain prose with no `<`, `>`, or `&` sequences. Enforced at the shipping plans' verify gates and by React's own escaping of the JSON payload. |
| T-02-09 | Information Disclosure | An open-question block quoting production data or an unpublished guild identity into the committed draft | medium | mitigate | Section G references guilds only by the name already public on the homepage today. The draft copies nothing from the raw Management API response, which plan 02-01 kept out of any file. |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no packages. 02-RESEARCH.md marks the Package Legitimacy Audit not applicable. |

Canon-referral breadcrumbs (not minted as prohibitions): injection through authored JSON-LD strings is canon OWASP territory and is covered by `/gsd-secure-phase` and eslint; it is tracked here only as threat T-02-08.
</threat_model>

<verification>
- `02-COPY-DRAFT.md` contains all seven sections, at least 13 well-formed `RESIDUAL-ALLOWANCE` lines, and zero em dashes.
- After the checkpoint, the artifact's first line reads `STATUS: APPROVED` and a line matching `^SIGN-OFF: APPROVED ` is present.
- Every section has an `### Approved` subsection holding the final strings verbatim.
- `git status --porcelain -- app public README.md` is empty for the whole plan. No shipping surface changes in wave 2.
</verification>

<success_criteria>
A human has read and approved every user-facing string this phase will ship, in one pass, and the approved strings exist verbatim in a committed artifact that the shipping plans can copy from and gate on.
</success_criteria>

<artifacts_this_phase_produces>
Introduced by this plan:

- `.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md` (new file)
  - `STATUS: DRAFT, PENDING SIGN-OFF` and `STATUS: APPROVED` first-line markers
  - `SIGN-OFF: APPROVED <ISO date>` marker line
  - `RESIDUAL-ALLOWANCE: <path> = <N>` marker lines, one per swept file
  - Sections `## A. Signup copy (COPY-01)` through `## G. Open questions for sign-off`
  - `### Approved` subsections holding final verbatim strings
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-checkable-conversion-copy/02-02-SUMMARY.md` when done
</output>
