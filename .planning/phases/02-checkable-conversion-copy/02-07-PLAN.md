---
phase: 02-checkable-conversion-copy
plan: 07
type: execute
wave: 5
depends_on: ["02-04", "02-05", "02-06"]
files_modified:
  - app/blog/page.tsx
  - app/blog/how-to-run-loot-without-a-spreadsheet/page.tsx
  - app/blog/how-to-set-up-a-fair-loot-system-for-your-wow-guild/page.tsx
  - app/components/landing/BlogRelatedPosts.tsx
autonomous: true
requirements: [PROOF-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The two blog posts carry exactly the disposition the user chose for the indexed-title question: either the full change across title, H1, and structured-data headline, or the narrower metadata-only change, never a partial mix of the two (D-12)"
    - "Wherever one blog post's title string appears, it is the same string: the page title, the openGraph title, the structured-data headline, the breadcrumb or item-list name, the analytics tracker prop, the on-page H1, the blog index card, and the related-posts card all agree"
    - "Every remaining occurrence of the narrower positioning phrase in each touched file equals the count the user approved in that file's RESIDUAL-ALLOWANCE line"
    - "Across all 13 files in the repositioning inventory, the total residual count equals the sum of the approved allowances, so the sweep is provably whole rather than provably started (D-12)"
    - "Every shipped string byte-matches its APPROVED-STRING value in 02-COPY-DRAFT.md (D-16)"
    - "No positioning string on any touched surface claims Retail or current-expansion support (D-13)"
    - "No em dash appears in any source file changed anywhere in this phase"
    - "The full test suite, typecheck, and lint pass on the complete phase diff, not just on this plan's files"
  artifacts:
    - path: "app/blog/how-to-run-loot-without-a-spreadsheet/page.tsx"
      provides: "Post title, openGraph title, structured-data headline, item-list name, tracker title prop, and on-page H1, all agreeing"
      contains: "BlogTracker"
    - path: "app/blog/how-to-set-up-a-fair-loot-system-for-your-wow-guild/page.tsx"
      provides: "Post meta description, both copies"
      contains: "description"
    - path: "app/blog/page.tsx"
      provides: "Blog index card title mirroring the post title"
      contains: "title:"
    - path: "app/components/landing/BlogRelatedPosts.tsx"
      provides: "Related-post card title mirroring the post title"
      contains: "title:"
  key_links:
    - from: "app/blog/how-to-run-loot-without-a-spreadsheet/page.tsx"
      to: "app/blog/page.tsx"
      via: "the blog index card duplicates the post title string; changing one without the other ships two names for one article"
      pattern: "Without a Spreadsheet"
    - from: "app/blog/how-to-run-loot-without-a-spreadsheet/page.tsx"
      to: "app/components/landing/BlogRelatedPosts.tsx"
      via: "the related-posts card duplicates the same post title string"
      pattern: "Without a Spreadsheet"
  flagged_assumptions:
    - "PLANNER ASSUMPTION (indexed-title risk): 02-RESEARCH.md Open Question 2 flags that three of the six occurrences in the how-to-run-loot post are its indexed page title, its on-page H1, and its structured-data headline, and that changing a ranking title carries more search volatility than changing a meta description. The planner does not choose; section G question 1 of the approved draft records the user's answer, and this plan applies exactly that answer. Flagged, not resolved by the planner."
    - "PLANNER ASSUMPTION (title mirrors): the post title string is duplicated in at least four places outside its own metadata block (the index card, the related-posts card, the tracker prop, the item-list name). Planner assumption: a title change must move all of them in one commit, since a partial change ships two names for one article and splits its own click signal. Flagged, not resolved."
---

<!-- planner-discipline-allow: WoW Classic -->
<!-- planner-discipline-allow: World of Warcraft Classic -->

<objective>
Finish the repositioning on the blog surfaces exactly as the user decided, keep every copy of a post title in agreement, and prove the whole 13-file sweep landed as one consistent state.

**As a** reader arriving from search on a blog post, **I want to** see the same article name in the search result, the page heading, the index, and the related links, **so that** I am confident I landed where I meant to.

Purpose: the blog carries the highest-risk part of the D-12 sweep, because three of the six occurrences in one post are its indexed title, its on-page H1, and its structured-data headline. The user answered that question at the sign-off; this plan applies the answer without reinterpreting it. It is also the phase's closing plan, so it runs the one check no earlier plan could: that all 13 files in the inventory add up to the approved total, which is what makes the all-or-nothing broadening provably whole.

Output: consistent blog titles and a verified, complete sweep.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/loot-list-plus/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/loot-list-plus/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md
@.planning/phases/02-checkable-conversion-copy/02-COPY-AUDIT.md
@.planning/phases/02-checkable-conversion-copy/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the approved blog disposition to both posts</name>
  <precondition>`.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md` contains a line matching `^SIGN-OFF: APPROVED ` and section G question 1 carries a recorded answer on the indexed-title scope.</precondition>
  <reversibility rating="costly">Changing an already-indexed page title, H1, and structured-data headline moves a ranking URL's primary signal, and reverting it means a second round of ranking volatility on the same URL inside a sprint window that closes Sep 24. The user answered this at the D-16 sign-off, so no gate is re-asked here.</reversibility>
  <files>app/blog/how-to-run-loot-without-a-spreadsheet/page.tsx, app/blog/how-to-set-up-a-fair-loot-system-for-your-wow-guild/page.tsx</files>
  <read_first>
    - `app/blog/how-to-run-loot-without-a-spreadsheet/page.tsx`. Six occurrences of the narrower phrase: the Metadata `title` at line 10, the `openGraph.title` at line 30, the structured-data `headline` at line 45, an item name at line 101, the `BlogTracker` `title` prop at line 121, and the on-page H1 at line 139.
    - `app/blog/how-to-set-up-a-fair-loot-system-for-your-wow-guild/page.tsx`. Two occurrences, both meta description strings, at lines 11 and 44.
    - `.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md`, section G question 1 (the recorded answer), section E's disposition rows for both files, and every `APPROVED-STRING: repos.app/blog/*` line
  </read_first>
  <action>
    Read the recorded answer to section G question 1 first, and let it decide the scope before touching either file. If the answer is metadata only, change the meta description strings and leave the page title, the on-page H1, and the structured-data headline byte-identical. If the answer is the full change, move all six occurrences in the how-to-run-loot post together in one commit: an article whose page title says one thing, whose H1 says another, and whose structured-data headline says a third is worse than either consistent state. Do not invent a middle option.

    Take every replacement string verbatim from its `APPROVED-STRING: repos.app/blog/<path>.<line>` line. An occurrence whose approved disposition is keep stays byte-identical.

    In `app/blog/how-to-set-up-a-fair-loot-system-for-your-wow-guild/page.tsx` the same description sentence exists twice, at lines 11 and 44. Move both or neither.

    Change no article body copy beyond the occurrences the approved table names, no component structure, and no styling.
  </action>
  <verify>
    <automated>npm run typecheck && for F in app/blog/how-to-run-loot-without-a-spreadsheet/page.tsx app/blog/how-to-set-up-a-fair-loot-system-for-your-wow-guild/page.tsx; do KEY=$(printf '%s' "$F" | sed 's/[].[^$\\*\/()]/\\&/g'); N=$(grep -oE "^RESIDUAL-ALLOWANCE: ${KEY} = [0-9]+$" .planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md | grep -oE '[0-9]+$'); test -n "$N" || { echo "no RESIDUAL-ALLOWANCE recorded for $F"; exit 1; }; A=$(grep -cE 'WoW Classic|World of Warcraft Classic' "$F" || true); test "$A" = "$N" || { echo "$F has $A residual occurrences, approved allowance is $N"; exit 1; }; done; D=$(grep -c "$(sed -n '11p' app/blog/how-to-set-up-a-fair-loot-system-for-your-wow-guild/page.tsx | sed 's/^ *//;s/[^A-Za-z .,]//g' | cut -c1-40)" app/blog/how-to-set-up-a-fair-loot-system-for-your-wow-guild/page.tsx); test "$D" -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - The scope applied matches the recorded answer to section G question 1, and the SUMMARY quotes that answer.
    - For each of the two post files, the residual occurrence count equals that file's `RESIDUAL-ALLOWANCE` number, and a missing allowance line fails the check by name.
    - In `app/blog/how-to-run-loot-without-a-spreadsheet/page.tsx`, either all six occurrences changed or none of the title, H1, and headline trio changed; there is no state where the page title and the H1 disagree.
    - In `app/blog/how-to-set-up-a-fair-loot-system-for-your-wow-guild/page.tsx`, the duplicated description sentence is identical in both places.
    - Every `APPROVED-STRING: repos.app/blog/*` value appears verbatim in the corresponding file.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Both posts carry exactly the disposition the user chose, with no internal disagreement inside either file.</done>
</task>

<task type="auto">
  <name>Task 2: Keep every external copy of a post title in agreement with the post</name>
  <files>app/blog/page.tsx, app/components/landing/BlogRelatedPosts.tsx</files>
  <read_first>
    - `app/blog/how-to-run-loot-without-a-spreadsheet/page.tsx` as changed by task 1, specifically its final Metadata `title` value
    - `app/blog/page.tsx` line 62, the index card `title` for that post
    - `app/components/landing/BlogRelatedPosts.tsx` line 28, the related-post card `title` for the same post
    - `.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md`, section E rows for these two files
  </read_first>
  <action>
    The post title string is duplicated outside its own file in two places: the blog index card and the related-posts card. Set both to the exact final title string task 1 produced, character for character. If task 1's recorded answer left the post title unchanged, then these two also stay unchanged, and the check below still confirms all three agree.

    Do not retype the title from memory or from the approved draft; read the final value out of `app/blog/how-to-run-loot-without-a-spreadsheet/page.tsx` and copy it, so the three copies cannot drift by a character.

    Change nothing else in either file: no slug, no date, no excerpt, no ordering, no styling.
  </action>
  <verify>
    <automated>npm run typecheck && T=$(node -e "const s=require('fs').readFileSync('app/blog/how-to-run-loot-without-a-spreadsheet/page.tsx','utf8');const m=s.match(/title:\s*'([^']+)'/)||s.match(/title:\s*\"([^\"]+)\"/);if(!m){console.error('could not read post title');process.exit(1)}process.stdout.write(m[1])") && test -n "$T" && grep -qF -- "$T" app/blog/page.tsx && grep -qF -- "$T" app/components/landing/BlogRelatedPosts.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The first `title` value read out of `app/blog/how-to-run-loot-without-a-spreadsheet/page.tsx` appears verbatim in both `app/blog/page.tsx` and `app/components/landing/BlogRelatedPosts.tsx`; the check fails loudly if the title cannot be read rather than passing on an empty match.
    - `git diff app/blog/page.tsx app/components/landing/BlogRelatedPosts.tsx` shows changes to `title` values only, with no slug, date, excerpt, ordering, or styling change.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>One article has one name, everywhere it appears.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the whole phase landed as one consistent state</name>
  <files>(none, runs phase-wide gates only across every file in the repositioning inventory)</files>
  <read_first>
    - `.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md`, every `RESIDUAL-ALLOWANCE` line and every `APPROVED-STRING` line
    - `.planning/phases/02-checkable-conversion-copy/02-03-SUMMARY.md`, the four reusable gate commands
    - The SUMMARY files for plans 02-04, 02-05, and 02-06, for any disposition those plans recorded as escalated rather than applied
  </read_first>
  <action>
    This is the only point in the phase where every touched file exists in its final state at once, so run the checks no earlier plan could.

    Sweep completeness. For each of the 13 files named by a `RESIDUAL-ALLOWANCE` line, compare the file's actual residual occurrence count of the narrower positioning phrase against its approved allowance, and also compare the repo-wide total against the sum of the allowances. A per-file pass with a wrong total means a file was missed from the inventory, which is exactly the half-applied state D-12 forbids. Report both numbers, not just a boolean.

    Approved-string parity across the whole phase. Run the parity loop over every `APPROVED-STRING` line with no key filter, checking each value against the full set of files this phase touched. Name every key that is not found and fail; do not let a missing key default to a pass.

    Em dash. Run the diff-scoped grep across every file changed in the phase except the planning directory, using the phase base commit as the diff base. `.planning/**` documents are committed too and would otherwise poison the result. There is no lint rule for this in the repo, so this grep is the only enforcement that exists.

    Retail claim. Grep the whole in-repo user-facing surface for any compatibility claim naming Retail or a current-expansion product name, and confirm there is none. D-13 makes this a hard line and no approved string should have introduced one.

    Suite health. Run `npm test`, `npm run typecheck`, and `npm run lint` on the complete phase diff.

    Outstanding work. Read section F of the approved draft and restate the founder-owned handoff checklist in the SUMMARY, so the phase closes with the user knowing exactly which external surfaces still carry the old wording and who changes them. Per D-14 no executor touches them.
  </action>
  <verify>
    <automated>DRAFT=.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md; SUM_ALLOWED=0; SUM_ACTUAL=0; FAIL=0; while IFS= read -r line; do F=$(printf '%s' "$line" | sed -E 's/^RESIDUAL-ALLOWANCE: (.+) = [0-9]+$/\1/'); N=$(printf '%s' "$line" | grep -oE '[0-9]+$'); test -f "$F" || { echo "allowance names a missing file: $F"; FAIL=1; continue; }; A=$(grep -cE 'WoW Classic|World of Warcraft Classic' "$F" || true); SUM_ALLOWED=$((SUM_ALLOWED+N)); SUM_ACTUAL=$((SUM_ACTUAL+A)); test "$A" = "$N" || { echo "$F: actual $A, allowed $N"; FAIL=1; }; done < <(grep -E '^RESIDUAL-ALLOWANCE: .+ = [0-9]+$' "$DRAFT"); echo "sweep totals: actual=$SUM_ACTUAL allowed=$SUM_ALLOWED"; test "$SUM_ACTUAL" = "$SUM_ALLOWED" || FAIL=1; MISSING=0; while IFS= read -r line; do key=${line#APPROVED-STRING: }; key=${key%% = *}; val=${line#*" = "}; grep -rqF -- "$val" app public README.md || { echo "unshipped approved string: $key"; MISSING=1; }; done < <(grep '^APPROVED-STRING: ' "$DRAFT"); BASE=$(git rev-parse HEAD~1); CHANGED=$(git diff --name-only "$BASE"...HEAD | grep -v '^\.planning/' || true); if [ -n "$CHANGED" ]; then echo "$CHANGED" | xargs -r grep -n '—' && { echo "em dash found in changed source"; FAIL=1; }; fi; grep -rniE 'works with retail|retail support|supports retail|for retail wow' app public README.md && { echo "Retail claim found"; FAIL=1; }; test "$FAIL" -eq 0 && test "$MISSING" -eq 0 && npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Every file named by a `RESIDUAL-ALLOWANCE` line exists, and its actual residual count equals its allowance.
    - The repo-wide residual total equals the sum of the allowances, and both numbers are printed in the output rather than collapsed into a pass or fail.
    - Every `APPROVED-STRING` value in the draft, with no key filter, is found verbatim somewhere under `app/`, `public/`, or `README.md`; every key that is not found is named and the check exits non-zero.
    - The em-dash grep over the diff-scoped, non-planning changed files produces no output.
    - No Retail or current-expansion compatibility claim exists anywhere under `app/`, `public/`, or `README.md`.
    - `npm test`, `npm run typecheck`, and `npm run lint` all exit 0.
    - The SUMMARY restates section F's founder-owned handoff checklist item by item and names any disposition an earlier plan escalated instead of applying.
  </acceptance_criteria>
  <done>The 13-file sweep is provably whole, every approved string is on a real page, no Retail claim exists, the suite is green, and the only outstanding work is the founder-owned external checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| indexed blog title to search engines | A ranking URL's primary signal. Changing it moves rankings; changing it inconsistently splits them. |
| duplicated title string across four files | The same article name exists in several places with no single source; drift ships two names for one article. |
| approved artifact to committed source | Commits land on main and auto-deploy. This is the phase's last chance to catch drift before it is public. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-23 | Repudiation | A partially applied blog change where the page title, the H1, and the structured-data headline disagree | medium | mitigate | Task 1 applies the recorded answer as an all-or-nothing scope, and an acceptance criterion forbids any state where the title and the H1 disagree. Task 2 copies the final title from the post itself rather than retyping it. |
| T-02-18 | Repudiation | A half-applied repositioning sweep across the 13 inventory files | medium | mitigate | Task 3 compares both the per-file counts and the repo-wide total against the approved allowances, so a file missing from the inventory fails the total even when every listed file passes. |
| T-02-17 | Spoofing | A Retail or current-expansion compatibility claim reaching production | high | mitigate | Task 3 greps the entire in-repo user-facing surface for compatibility phrasing naming Retail and fails on any hit. D-13 makes this a hard line. |
| T-02-10 | Tampering | Copy drift between the approved artifact and the committed source anywhere in the phase | high | mitigate | Task 3 runs the parity loop over every approved key with no filter and names each one it cannot find. |
| T-02-22 | Elevation of Privilege | Editing a founder-owned external surface | medium | mitigate | D-14 forbids it. Task 3 restates the handoff checklist for the user instead of acting on it. |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no packages. 02-RESEARCH.md marks the Package Legitimacy Audit not applicable. |
</threat_model>

<verification>
- Per-file and repo-wide residual counts both equal the approved allowances, with both totals printed.
- Every `APPROVED-STRING` value in the draft is present verbatim under `app/`, `public/`, or `README.md`.
- Diff-scoped em-dash grep excluding `.planning/` produces no output.
- No Retail compatibility claim exists in the repo's user-facing surface.
- One article has one title across the post, the index, and the related-posts card.
- `npm test`, `npm run typecheck`, `npm run lint` all exit 0.
</verification>

<success_criteria>
The blog reflects exactly the decision the user made about its indexed titles, every copy of an article name agrees, and the phase can be shown to have landed as one consistent state rather than a partially applied one.
</success_criteria>

<artifacts_this_phase_produces>
Modified by this plan (no new symbols or files):

- `app/blog/how-to-run-loot-without-a-spreadsheet/page.tsx`: `metadata.title`, `metadata.openGraph.title`, the structured-data `headline`, the item-list name, the `BlogTracker` `title` prop, and the on-page H1, per the recorded scope answer
- `app/blog/how-to-set-up-a-fair-loot-system-for-your-wow-guild/page.tsx`: both copies of the meta description sentence
- `app/blog/page.tsx`: the index card `title` for the how-to-run-loot post
- `app/components/landing/BlogRelatedPosts.tsx`: the related-post card `title` for the same post
- Phase-closing gate output recorded in `02-07-SUMMARY.md`: per-file and total residual counts, the unfiltered approved-string parity result, and the restated founder-owned handoff checklist
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-checkable-conversion-copy/02-07-SUMMARY.md` when done
</output>
