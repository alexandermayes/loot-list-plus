---
phase: 02-checkable-conversion-copy
plan: 05
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - app/layout.tsx
  - app/(landing)/landing/page.tsx
  - app/changelog/layout.tsx
  - app/compare/page.tsx
autonomous: true
requirements: [PROOF-02]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The site-wide root metadata and its JSON-LD graph carry the broadened positioning wording, so no page inherits the narrower framing through the title template or the default description (D-12)"
    - "The homepage title leads with the words officers actually search (loot list, attendance, Loot Score) with the brand attached, and the on-page hero H1 is untouched (D-10)"
    - "The changelog title and description read as unambiguously changelog intent and the page is still indexable (D-09)"
    - "The compare title and description are built from checkable claims rather than marketing superlatives, aimed at competitor queries (D-11)"
    - "On every page touched here, title, description, openGraph title and description, twitter title and description, and where present jsonLd headline and description all changed together as one copy unit, closing rather than reproducing the drift already present on the homepage file"
    - "Every remaining occurrence of the narrower positioning phrase in each touched file equals the count the user approved in that file's RESIDUAL-ALLOWANCE line, so nothing was over-swept and nothing was missed"
    - "Every shipped metadata string byte-matches its APPROVED-STRING value in 02-COPY-DRAFT.md (D-16)"
    - "No positioning string on any touched surface claims Retail or current-expansion support (D-13)"
    - "No JSON-LD string value contains an angle bracket or a script-closing sequence"
    - "No em dash appears in any source file changed by this plan"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Root Metadata title template default, description, keywords array, openGraph image alt, and the two JSON-LD graph node descriptions"
      contains: "metadataBase"
    - path: "app/(landing)/landing/page.tsx"
      provides: "Homepage title, description, openGraph and twitter blocks, image alt"
      contains: "alternates"
    - path: "app/changelog/layout.tsx"
      provides: "Changelog title, description, openGraph block"
      contains: "canonical"
    - path: "app/compare/page.tsx"
      provides: "Compare title, description, keywords, openGraph block, and the Article JSON-LD headline and description"
      contains: "jsonLd"
  key_links:
    - from: "app/layout.tsx"
      to: "app/(landing)/landing/page.tsx"
      via: "the root title template and default description are inherited by any page that does not override them, so a half-applied sweep here leaks the old framing site wide"
      pattern: "template:"
    - from: "app/compare/page.tsx"
      to: "app/compare/page.tsx"
      via: "metadata.description, openGraph.description, and jsonLd.description are three copies of one sentence and must move together"
      pattern: "jsonLd"
  flagged_assumptions:
    - "PLANNER ASSUMPTION (keywords array): `app/layout.tsx` line 40 mixes the narrow and broad forms today. Per the user's added note in CONTEXT.md, the shorthand should stay present alongside the full name because both are real search terms, so the planner expects this array to be a deliberate keep rather than a replace, recorded per occurrence in the approved disposition table. Flagged, not resolved; the RESIDUAL-ALLOWANCE line for this file is what settles it."
    - "PLANNER ASSUMPTION (domain routing): 02-RESEARCH.md Open Question 1 notes that the mapping from the getlootlist.com root to the /landing route lives outside this repo, with no middleware or host rewrite in next.config.ts. Planner assumption: file identity is what this plan needs and it is confirmed, so the routing mechanism is out of scope. Flagged, not resolved."
---

<!-- planner-discipline-allow: WoW Classic -->
<!-- planner-discipline-allow: World of Warcraft Classic -->

<objective>
Make the four highest-value search surfaces say what officers actually search for, in the broadened positioning wording, with no unverifiable superlatives left in them.

**As an** officer searching for a loot list tool, **I want to** see a result whose title tells me this is the category I am looking for, **so that** I click it instead of scrolling past.

Purpose: three separate problems converge on the same four files. The homepage is losing the generic query to its own changelog (514 of 597 impressions at 0.19 percent CTR). The compare page ranks around position 9 to 12.6 on competitor queries with 0 to 1.31 percent CTR and reads to an AI crawler as vendor marketing. And the root layout is the first stop in the repositioning sweep because its description and JSON-LD graph cascade into every page that does not override them, which is exactly how a half-applied broadening happens.

Output: four metadata blocks whose every field moved together, in the approved wording.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/loot-list-plus/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/loot-list-plus/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md
@.planning/phases/02-checkable-conversion-copy/02-COPY-AUDIT.md
@.planning/phases/02-checkable-conversion-copy/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep the root layout first, metadata and JSON-LD graph together</name>
  <precondition>`.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md` contains a line matching `^SIGN-OFF: APPROVED ` and a line matching `^RESIDUAL-ALLOWANCE: app/layout\.tsx = [0-9]+$`.</precondition>
  <reversibility rating="costly">D-12 touches 13 files and triggers the standing full-surface sweep rule. A state where marketing pages broaden but the site-wide JSON-LD does not is worse than either consistent state, so undoing a partial application means revisiting every surface. The user chose this in CONTEXT.md; no gate is re-asked.</reversibility>
  <files>app/layout.tsx</files>
  <read_first>
    - `app/layout.tsx` in full. The six occurrences of the narrower phrase are at lines 33 (`metadata.title.default`), 39 (`metadata.description`), 40 (the `keywords` array), 54 (the openGraph image `alt`), 100 and 130 (two JSON-LD `@graph` node descriptions). Note the existing inline comment above the description declaring it the one definitive product description to be kept identical across the homepage, the About page, the GitHub README, JSON-LD, and directory listings.
    - `.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md`, section E's disposition rows for `app/layout.tsx` and every `APPROVED-STRING: repos.app/layout.tsx.*` line
    - `.planning/phases/02-checkable-conversion-copy/02-COPY-AUDIT.md`, section `## Repositioning Inventory`
  </read_first>
  <action>
    Apply the approved disposition to each of the six occurrences in `app/layout.tsx`, one at a time, taking replacement text verbatim from the matching `APPROVED-STRING: repos.app/layout.tsx.<line>` value. Occurrences whose disposition is keep are left byte-identical; the `keywords` array at line 40 is expected to be one of those, because both the shorthand and the full name are real search terms, but the approved disposition table is what decides, not this instruction.

    Treat the inline comment above the description as a live checklist, not decoration: it names the homepage, the About page, the README, JSON-LD, and directory listings as surfaces that must carry an identical product description. Plan 02-06 owns `/about` and `README.md`, and the JSON-LD is in this file, so after editing here, confirm the approved replacement description is the same string those other plans are told to use, and flag any divergence in the SUMMARY rather than letting two definitive descriptions exist.

    Update both JSON-LD `@graph` node descriptions at lines 100 and 130 in the same pass as the plain metadata. Crawlers read JSON-LD independently of visible page text, so leaving these behind is the exact half-applied failure D-12 forbids. Keep every JSON-LD string value plain prose with no angle brackets and no ampersand sequences; these objects are serialized into a script tag.

    Change no other field. `metadataBase`, the image dimensions, `locale`, `type`, the twitter card type, and every non-copy value stay as they are.
  </action>
  <verify>
    <automated>npm run typecheck && F=app/layout.tsx; N=$(grep -oE "^RESIDUAL-ALLOWANCE: app/layout\.tsx = [0-9]+$" .planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md | grep -oE '[0-9]+$'); test -n "$N" || { echo "no RESIDUAL-ALLOWANCE recorded for $F"; exit 1; }; A=$(grep -cE 'WoW Classic|World of Warcraft Classic' "$F"); test "$A" = "$N" || { echo "$F has $A residual occurrences, approved allowance is $N"; exit 1; }; ! grep -nE '"[^"]*[<>][^"]*"' app/layout.tsx | grep -qE 'description|headline'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'WoW Classic|World of Warcraft Classic' app/layout.tsx` equals the number in that file's `RESIDUAL-ALLOWANCE` line, and the check fails loudly and names the file if no allowance line exists.
    - Every `APPROVED-STRING: repos.app/layout.tsx.*` value appears verbatim in the file.
    - Both JSON-LD `@graph` node descriptions (previously at lines 100 and 130) changed in the same commit as `metadata.description`.
    - No `description` or `headline` string value in this file contains `<` or `>`.
    - `git diff app/layout.tsx` shows no change to `metadataBase`, to any image URL or dimension, to `locale`, or to any `type` field.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The root metadata and its JSON-LD graph carry the approved positioning wording, and the residual count matches what the user approved.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the query cannibalization by authoring the homepage and changelog titles together</name>
  <precondition>`.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md` contains lines matching `^RESIDUAL-ALLOWANCE: app/\(landing\)/landing/page\.tsx = [0-9]+$` and `^RESIDUAL-ALLOWANCE: app/changelog/layout\.tsx = [0-9]+$`.</precondition>
  <files>app/(landing)/landing/page.tsx, app/changelog/layout.tsx</files>
  <read_first>
    - `app/(landing)/landing/page.tsx` lines 20 to 50. Seven occurrences of the narrower phrase live across `title`, `description`, `openGraph.title`, `openGraph.description`, the openGraph image `alt`, `twitter.title`, and `twitter.description`. Note the pre-existing drift: the plain `description` and the `openGraph.description` are two different sentences, and the openGraph one is the more superlative of the two.
    - `app/changelog/layout.tsx` in full. Narrower shape, no keywords and no JSON-LD, with `title`, `description`, `alternates.canonical`, and an `openGraph` block.
    - `.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md`, section D blocks for the homepage and the changelog, and every `APPROVED-STRING: meta.landing.*` and `APPROVED-STRING: meta.changelog.*` line
  </read_first>
  <action>
    These two files are the two ends of one problem, so author them in a single task rather than independently. /changelog currently absorbs 514 of 597 impressions on the top generic query at 0.19 percent CTR while the homepage does not rank for it. D-10 gives the homepage the category-forward title so it can claim that intent, and D-09 gives the changelog an unambiguously changelog-intent title and description so it stops competing. Neither gets a noindex.

    For `app/(landing)/landing/page.tsx`, replace `title`, `description`, `openGraph.title`, `openGraph.description`, the openGraph image `alt`, `twitter.title`, and `twitter.description` with the matching `APPROVED-STRING: meta.landing.*` values, all in one edit. Do not leave the openGraph and twitter descriptions carrying a different, more superlative sentence than the plain description; closing that drift is part of this task, and an unverifiable superlative in an openGraph description is a PROOF-02 finding in its own right. Leave `alternates.canonical`, the image URL and dimensions, `siteName`, `locale`, `type`, and the twitter card type untouched.

    The on-page hero H1 is not in this file and must not be touched by any part of this phase. It lives in `app/components/landing/LandingHero.tsx` and D-10 declares it sacred.

    For `app/changelog/layout.tsx`, replace `title`, `description`, `openGraph.title`, and `openGraph.description` with the matching `APPROVED-STRING: meta.changelog.*` values. Leave `alternates.canonical` and the openGraph `url` and `type` untouched, and add no `robots` directive; D-09 explicitly rules out a noindex here.
  </action>
  <verify>
    <automated>npm run typecheck && for F in "app/(landing)/landing/page.tsx" "app/changelog/layout.tsx"; do KEY=$(printf '%s' "$F" | sed 's/[].[^$\\*\/()]/\\&/g'); N=$(grep -oE "^RESIDUAL-ALLOWANCE: ${KEY} = [0-9]+$" .planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md | grep -oE '[0-9]+$'); test -n "$N" || { echo "no RESIDUAL-ALLOWANCE recorded for $F"; exit 1; }; A=$(grep -cE 'WoW Classic|World of Warcraft Classic' "$F" || true); test "$A" = "$N" || { echo "$F has $A residual occurrences, approved allowance is $N"; exit 1; }; done; ! grep -q 'robots' app/changelog/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - For each of the two files, the residual occurrence count equals that file's `RESIDUAL-ALLOWANCE` number, and a missing allowance line fails the check by name.
    - Every `APPROVED-STRING: meta.landing.*` and `APPROVED-STRING: meta.changelog.*` value appears verbatim in the corresponding file.
    - `app/(landing)/landing/page.tsx` `description`, `openGraph.description`, and `twitter.description` all changed in this commit, and none of them still carries the pre-existing superlative sentence that differed from the plain description.
    - `app/changelog/layout.tsx` contains no `robots` key.
    - `git diff` shows no change to `app/components/landing/LandingHero.tsx`; this plan never touches the sacred hero H1.
    - `git diff` shows no change to any `alternates.canonical`, image URL, image dimension, `siteName`, `locale`, or card `type` value.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The homepage claims the generic query intent, the changelog reads as a changelog, and both carry the approved positioning wording.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the compare page for competitor queries and run the gate battery</name>
  <precondition>`.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md` contains a line matching `^RESIDUAL-ALLOWANCE: app/compare/page\.tsx = [0-9]+$`.</precondition>
  <files>app/compare/page.tsx</files>
  <read_first>
    - `app/compare/page.tsx` lines 1 to 45. Three occurrences of the narrower phrase live in `metadata.description` (line 10), `openGraph.description` (line 26), and the Article `jsonLd.description` (line 39). The file also has a `keywords` array and a `jsonLd` object whose `headline` mirrors the metadata title. Line 32 defines `const APP_URL = 'https://www.getlootlist.com'`.
    - `.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md`, section D's compare block, section G question 3, and every `APPROVED-STRING: meta.compare.*` line
    - `scripts/analytics/ai-answer-log.csv`, the week-1 run, for the finding that a model citing this page flagged it as vendor-written marketing
    - `.planning/phases/02-checkable-conversion-copy/02-03-SUMMARY.md`, the four reusable gate commands
  </read_first>
  <action>
    Replace `metadata.title`, `metadata.description`, `openGraph.title`, `openGraph.description`, `jsonLd.headline`, and `jsonLd.description` with the matching `APPROVED-STRING: meta.compare.*` values in one edit. This file holds three separate copies of description-style text and they must move in lockstep or the page ships three different claims about itself. Update the `keywords` array only if the approved draft changed it.

    D-11's target is a snippet that earns a click from a competitor query using checkable claims. The evidence behind it is in the draft: position roughly 9 to 12.6 with 0 to 1.31 percent CTR on those queries, and a week-1 AI answer run in which a model cited this page in 4 of 6 answers while calling it vendor-written marketing. Ship the approved wording, not a new interpretation of that brief.

    Keep every JSON-LD string value plain prose with no angle brackets and no ampersand sequences, since `jsonLd` is serialized into a script tag on this page.

    Apply the user's approved answer to section G question 3 about `APP_URL` at line 32, and only that answer. Every sibling file that defines this constant (`LandingNav.tsx`, `LandingHero.tsx`, `app/about/page.tsx`, `app/pricing/page.tsx`) points at the signup domain while this one points at the marketing domain, so the create-your-guild call to action on this page currently returns a reader to the marketing homepage. If the approved answer is to fix it, change the constant only; if the approved answer is to leave it, change nothing and record the reason in the SUMMARY. Do not decide this on your own.

    Then run the gate battery recorded in `02-03-SUMMARY.md`: the diff-scoped em-dash grep excluding the planning directory, the `APPROVED-STRING` parity loop scoped to the `meta.` and `repos.` keys for this plan's four files, and `npm test` plus `npm run typecheck` plus `npm run lint`.
  </action>
  <verify>
    <automated>npm run typecheck && F=app/compare/page.tsx; N=$(grep -oE "^RESIDUAL-ALLOWANCE: app/compare/page\.tsx = [0-9]+$" .planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md | grep -oE '[0-9]+$'); test -n "$N" || { echo "no RESIDUAL-ALLOWANCE recorded for $F"; exit 1; }; A=$(grep -cE 'WoW Classic|World of Warcraft Classic' "$F" || true); test "$A" = "$N" || { echo "$F has $A residual occurrences, approved allowance is $N"; exit 1; }; BASE=$(git rev-parse HEAD~1); CHANGED=$(git diff --name-only "$BASE"...HEAD | grep -v '^\.planning/' || true); if [ -n "$CHANGED" ]; then echo "$CHANGED" | xargs -r grep -n '—' && { echo "em dash found in changed source"; exit 1; }; fi; DRAFT=.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md; MISSING=0; while IFS= read -r line; do key=${line#APPROVED-STRING: }; key=${key%% = *}; val=${line#*" = "}; grep -qF -- "$val" app/layout.tsx "app/(landing)/landing/page.tsx" app/changelog/layout.tsx app/compare/page.tsx || { echo "unshipped approved string: $key"; MISSING=1; }; done < <(grep -E '^APPROVED-STRING: (meta\.(landing|changelog|compare)|repos\.app/layout\.tsx)' "$DRAFT"); test "$MISSING" -eq 0 && npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'WoW Classic|World of Warcraft Classic' app/compare/page.tsx` equals that file's `RESIDUAL-ALLOWANCE` number.
    - `metadata.description`, `openGraph.description`, and `jsonLd.description` all changed in this commit and are consistent with one another.
    - `jsonLd.headline` matches the approved `meta.compare.jsonld-headline` value verbatim.
    - No `description` or `headline` string value in this file contains `<` or `>`.
    - `APP_URL` at line 32 matches the user's recorded answer to section G question 3, and the SUMMARY names that answer.
    - The em-dash grep over the diff-scoped, non-planning changed files produces no output.
    - Every `APPROVED-STRING` value for this plan's four files is found verbatim; the check names any key it cannot find and exits non-zero.
    - `npm test`, `npm run typecheck`, and `npm run lint` all exit 0.
  </acceptance_criteria>
  <done>The compare page's three self-descriptions agree, read as checkable claims, and the plan's four files clear the phase gate battery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored metadata to search engines and AI crawlers | These strings are what a machine reads to decide what this product is. A wrong claim here propagates into answers the team does not control. |
| authored string to a JSON-LD script tag | `app/layout.tsx` and `app/compare/page.tsx` serialize authored objects into script elements. |
| approved artifact to committed source | Commits land on main and auto-deploy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering | Authored copy injected into the JSON-LD blocks in `app/layout.tsx` and `app/compare/page.tsx` | low | mitigate | Approved JSON-LD string values are restricted to plain prose. An acceptance criterion greps every description and headline value for `<` and `>`. React escapes the serialized payload, so this is defence in depth against a broken graph rather than a live XSS path. |
| T-02-17 | Spoofing | A broadened positioning string that a reader or an AI crawler reads as a Retail compatibility claim | high | mitigate | D-13 bans any Retail claim, and the wording was reviewed by a human at the D-16 sign-off. Feature-level claims stay expansion-specific and checkable. |
| T-02-18 | Repudiation | A half-applied sweep leaving the site-wide JSON-LD narrower than the visible marketing copy | medium | mitigate | Task 1 sweeps the root layout first, both JSON-LD node descriptions move in the same edit as the plain metadata, and each file's residual count is gated against an approved number rather than an assumption. |
| T-02-19 | Elevation of Privilege | Accidentally adding a `robots` noindex to `/changelog` while rewriting its metadata | low | mitigate | D-09 rules it out and an acceptance criterion greps the file for any `robots` key. |
| T-02-10 | Tampering | Copy drift between the approved artifact and the committed source | high | mitigate | Task 3's parity loop covers all four files and names any approved key it cannot find. Each task's precondition halts if the sign-off or the file's allowance line is absent. |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no packages. 02-RESEARCH.md marks the Package Legitimacy Audit not applicable. |
</threat_model>

<verification>
- For each of the four files, the residual occurrence count equals that file's approved `RESIDUAL-ALLOWANCE` number.
- Every `APPROVED-STRING` value scoped to these four files appears verbatim in the committed source.
- Diff-scoped em-dash grep excluding `.planning/` produces no output.
- No JSON-LD description or headline value contains an angle bracket.
- `app/changelog/layout.tsx` contains no `robots` key.
- `git diff` shows no change to `app/components/landing/LandingHero.tsx`.
- `npm test`, `npm run typecheck`, `npm run lint` all exit 0.
</verification>

<success_criteria>
The four surfaces most responsible for search and AI visibility carry the approved, broadened, superlative-free wording, with every mirrored metadata field moved as one copy unit and every residual occurrence accounted for by a number the user approved.
</success_criteria>

<artifacts_this_phase_produces>
Modified by this plan (no new symbols or files):

- `app/layout.tsx`: `metadata.title.default`, `metadata.description`, `metadata.keywords`, `metadata.openGraph.images[0].alt`, and the `description` fields of two JSON-LD `@graph` nodes
- `app/(landing)/landing/page.tsx`: `metadata.title`, `metadata.description`, `metadata.openGraph.title`, `metadata.openGraph.description`, `metadata.openGraph.images[0].alt`, `metadata.twitter.title`, `metadata.twitter.description`
- `app/changelog/layout.tsx`: `metadata.title`, `metadata.description`, `metadata.openGraph.title`, `metadata.openGraph.description`
- `app/compare/page.tsx`: `metadata.title`, `metadata.description`, `metadata.keywords`, `metadata.openGraph.title`, `metadata.openGraph.description`, `jsonLd.headline`, `jsonLd.description`, and `APP_URL` if the user approved the fix
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-checkable-conversion-copy/02-05-SUMMARY.md` when done
</output>
