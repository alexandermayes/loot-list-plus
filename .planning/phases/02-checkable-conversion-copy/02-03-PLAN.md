---
phase: 02-checkable-conversion-copy
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - app/components/LoginPage.tsx
  - app/page.tsx
  - app/components/__tests__/LoginPage.test.tsx
autonomous: true
requirements: [COPY-01]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An officer landing on the signup surface reads the approved officer-intent copy: page title, H1, body, primary CTA label, and secondary link text (COPY-01, D-07)"
    - "The secondary link resolves to the how-it-works section on the marketing domain from the signup domain, because its href is the absolute cross-domain URL https://www.getlootlist.com/#how-it-works and not a relative path (D-08)"
    - "Every shipped signup string byte-matches its APPROVED-STRING value in 02-COPY-DRAFT.md; nothing was reworded between approval and commit (D-16)"
    - "app/page.tsx still declares robots.index false, so the signup surface is not indexed and D-09, D-10, and D-11 do not apply to it"
    - "The Discord OAuth handler, the auth-failure Alert copy, and the invite-modal flow in LoginPage.tsx are unchanged; only the five COPY-01 strings and one href move"
    - "An automated test renders LoginPage and asserts each approved string and the absolute secondary href, so a future edit that silently reverts the copy fails the suite"
    - "No em dash appears in any source file changed by this plan"
    - "UI-SPEC E4 overflow: the H1, body, and CTA are fixed authored strings sized to fit container widths already proven by the currently shipped copy, and length is fixed at authoring time, so no dynamic overflow can occur"
    - "UI-SPEC E4 long-text: the signup copy is a fixed authored string chosen and visually verified at sign-off, not user-generated or dynamic-length content"
    - "UI-SPEC E5 long-text: the secondary link text is a fixed authored string chosen and visually verified at sign-off, not dynamic-length content"
    - "The three signup-scoped prohibitions recorded in plan 02-02 hold in the shipped strings: no unmeasured time-to-value claim, no urgency or shaming framing, and no implied Retail or current-expansion support"
  artifacts:
    - path: "app/components/LoginPage.tsx"
      provides: "Signup H1, body, primary CTA label, secondary link text and absolute cross-domain href"
      contains: "https://www.getlootlist.com/#how-it-works"
    - path: "app/page.tsx"
      provides: "Signup surface Metadata title and description, noindex preserved"
      contains: "index: false"
    - path: "app/components/__tests__/LoginPage.test.tsx"
      provides: "Render assertions for every approved signup string and the absolute secondary href"
      min_lines: 30
  key_links:
    - from: "app/components/LoginPage.tsx"
      to: "app/components/landing/LandingHowItWorks.tsx"
      via: "secondary link href targets the id=how-it-works section on the marketing domain across a domain boundary"
      pattern: "getlootlist\\.com/#how-it-works"
    - from: "app/components/__tests__/LoginPage.test.tsx"
      to: ".planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md"
      via: "the test's expected strings are the APPROVED-STRING values, copied verbatim at execution time"
      pattern: "getByRole|getByText"
  flagged_assumptions:
    - "EDGE PROBE (COPY-01, unclassified, unresolved): the probe could not classify this requirement, which is expected for static authored copy with no data shape. Planner assumption: the only edge-shaped risk on COPY-01 is the cross-domain secondary link resolving against the wrong origin (02-RESEARCH.md Pitfall 2), which the absolute href in D-08 closes and this plan's test asserts. Flagged, not resolved."
    - "PLANNER ASSUMPTION (test scope): the RTL test asserts rendered copy only. It does not exercise the Discord OAuth redirect, which needs a live provider, so a regression in the OAuth handler would not be caught here. Flagged, not resolved. The handler is untouched by this plan."
---

<objective>
Ship the approved signup copy end to end: metadata, rendered component text, the cross-domain link, and an automated test that pins all of it.

**As an** officer who just found LootList+, **I want to** land on the signup page and immediately understand what it does and what happens if I continue, **so that** I create a guild instead of bouncing to figure out what the product is.

Purpose: this is the phase's tracer. It drives one surface through every layer this phase touches (Next.js Metadata on the server, JSX copy in a client component, a cross-domain link, a Vitest render assertion, and the em-dash and approved-string gates) before the same pattern is applied to the testimonial block, four metadata blocks, and thirteen repositioning targets. If the gate battery or the test harness is wrong, it is wrong here on three files rather than after seventeen.

Output: a signup surface whose every visible string is the one the user approved, provably.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/loot-list-plus/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/loot-list-plus/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md
@.planning/phases/02-checkable-conversion-copy/02-UI-SPEC.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end approved signup copy, one surface, through metadata, component, link, and test</name>
  <precondition>`.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md` contains a line matching `^SIGN-OFF: APPROVED ` and at least one line matching `^APPROVED-STRING: signup\.`.</precondition>
  <files>app/components/LoginPage.tsx, app/page.tsx, app/components/__tests__/LoginPage.test.tsx</files>
  <read_first>
    - `.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md`, section A and its `### Approved` subsection, and every `APPROVED-STRING: signup.*` line
    - `app/components/LoginPage.tsx` in full. It is a client component; the copy targets are the H1 at line 145, the body paragraph immediately below it, the primary Button label at line 170, and the secondary anchor at lines 174 and 177. Everything else in the file, including the Discord OAuth handler, the invite-code fetch, the auth-failure Alert, and all `className` values, stays exactly as it is.
    - `app/page.tsx` lines 1 to 20, the `Metadata` export wrapping this component
    - `app/components/__tests__/ReserveItemPicker.test.tsx`, the structural analog for Vitest plus React Testing Library in this repo (render, `screen.getByText`, `screen.getByRole`); `vitest.config.ts` already provides jsdom, globals, and the `@/` alias project-wide
    - `app/components/landing/LandingHowItWorks.tsx` line 40, which is where `id="how-it-works"` actually lives
  </read_first>
  <action>
    Apply the approved signup copy to all three files in one pass, taking every string verbatim from the `APPROVED-STRING: signup.*` lines in `02-COPY-DRAFT.md`. Do not reword, retitle, recapitalize, or repunctuate an approved value. If an approved value is missing for any of the six keys `signup.title`, `signup.h1`, `signup.body`, `signup.cta`, `signup.secondary-text`, `signup.secondary-href`, stop and surface it rather than filling the gap.

    In `app/page.tsx`, replace the `metadata.title` value with `signup.title` and the `metadata.description` value with the approved description if one was recorded. Leave `alternates.canonical` and `robots` untouched: `index: false` is intentional on an authenticated app surface and must survive this plan.

    In `app/components/LoginPage.tsx`, replace the H1 text node with `signup.h1`, the body paragraph text node with `signup.body`, the primary Button label with `signup.cta`, and the secondary anchor's text node with `signup.secondary-text`. Change the secondary anchor's `href` to `signup.secondary-href`, which per D-08 is the absolute cross-domain URL pointing at the marketing domain's how-it-works section. It must be absolute: this component renders on lootlistplus.com and a relative anchor would resolve against an origin that does not serve that section. Keep every `className` on the H1, the paragraph, the Button, and the anchor exactly as it is; this is a text and href change, not a styling change.

    Create `app/components/__tests__/LoginPage.test.tsx` following the structure of the ReserveItemPicker test. Render `LoginPage` and assert, with the approved values inlined as expected strings: the H1 text is present as a level-1 heading, the body text is present, a button carrying the approved CTA label is present, a link carrying the approved secondary text is present, and that link's `href` attribute equals `signup.secondary-href` exactly. Add one assertion that the secondary link href starts with `https://` so a future relative-path regression fails loudly. Mock whatever `LoginPage` needs to render in jsdom (the Supabase client, `next/navigation`, and PostHog tracking) the same way the existing component tests in this repo handle their dependencies; the goal is a rendered tree, not an integration test of OAuth.

    Do not touch `app/components/landing/*`, any metadata file other than `app/page.tsx`, or any repositioning target. Those belong to plans 02-04 through 02-07 and share no files with this one.
  </action>
  <verify>
    <automated>npx vitest run app/components/__tests__/LoginPage.test.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run app/components/__tests__/LoginPage.test.tsx` passes and the file contains at least one assertion per approved signup string plus the href equality assertion.
    - `grep -c 'https://www.getlootlist.com/#how-it-works' app/components/LoginPage.tsx` returns 1.
    - `grep -c 'href="https://www.getlootlist.com"' app/components/LoginPage.tsx` returns 0, proving the bare marketing-root link was replaced by the anchored one.
    - `grep -q 'index: false' app/page.tsx` succeeds. The signup surface is still noindex.
    - For every `APPROVED-STRING: signup.<key> = <value>` line in `02-COPY-DRAFT.md`, `<value>` appears verbatim in `app/components/LoginPage.tsx` or `app/page.tsx`.
    - `git diff app/components/LoginPage.tsx` shows no change to any `className` value, to `handleDiscordLogin`, to the auth-failure Alert strings, or to the invite-code logic.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The signup surface renders the approved copy, its secondary link points across domains at the how-it-works section, and a passing test pins every string.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the phase gate battery on this surface</name>
  <files>(none, runs gates only against app/components/LoginPage.tsx and app/page.tsx)</files>
  <read_first>
    - `.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md`, every `APPROVED-STRING: signup.*` line
    - `.planning/phases/02-checkable-conversion-copy/02-UI-SPEC.md`, section "UI Considerations" elements E4 and E5, and section "Checker Sign-Off"
    - `git log --oneline -1` to identify the phase base commit for the diff-scoped greps
  </read_first>
  <action>
    Run and record the four gates that plans 02-04 through 02-07 will reuse verbatim, so any defect in the gate commands themselves surfaces on three files instead of on seventeen.

    Gate 1, em dash. There is no lint rule for this in the repo; PR #253 was a one-time manual sweep. Run the diff-scoped grep across every changed file except the planning directory, because `.planning/**` documents are committed too and would otherwise poison the result. Use the phase base commit as the diff base. Expect no output.

    Gate 2, approved-string parity. For each `APPROVED-STRING: signup.<key> = <value>` line in `02-COPY-DRAFT.md`, confirm `<value>` appears verbatim in one of this plan's two source files. Fail loudly and name the key if any value is absent, rather than defaulting a missing key to a pass.

    Gate 3, suite health. Run `npm test`, `npm run typecheck`, and `npm run lint` and record the result of each. These three are the per-wave gate for the rest of the phase.

    Gate 4, visual confirmation of the signup surface. Start the dev server in the background with `npm run dev`, wait for the ready signal, and confirm the root route responds before recording the human-check; never hand over a checkpoint against a server that is not up.

    Record all four gate commands verbatim in the plan SUMMARY under a heading the later plans can copy from. Change no source file in this task.
  </action>
  <verify>
    <automated>BASE=$(git rev-parse HEAD~1); CHANGED=$(git diff --name-only "$BASE"...HEAD | grep -v '^\.planning/' || true); if [ -n "$CHANGED" ]; then echo "$CHANGED" | xargs -r grep -n '—' && { echo "em dash found in changed source"; exit 1; }; fi; DRAFT=.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md; test -f "$DRAFT" || exit 1; MISSING=0; while IFS= read -r line; do key=${line#APPROVED-STRING: }; key=${key%% = *}; val=${line#*" = "}; grep -qF -- "$val" app/components/LoginPage.tsx app/page.tsx || { echo "unshipped approved string: $key"; MISSING=1; }; done < <(grep '^APPROVED-STRING: signup\.' "$DRAFT"); test "$MISSING" -eq 0 && npm test && npm run typecheck && npm run lint</automated>
    <human-check>
      The dev server is running at http://localhost:3000. Visit it and confirm on the signup surface:
      1. The H1, body paragraph, primary Discord button, and secondary link all read exactly the wording you approved.
      2. At a 375px viewport width, none of the four strings overflows its container, wraps into an ugly orphan, or pushes the Discord button off screen (UI-SPEC E4 overflow, E5 overflow).
      3. At a 1440px viewport width, the H1 still reads as one confident line or a deliberate two-line break, not an accidental one.
      4. Clicking the secondary link lands on the marketing site's how-it-works section, not the marketing homepage top.
      5. Clicking the Discord button still starts the OAuth flow. The copy change must not have touched it.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The em-dash grep over the diff-scoped, non-planning changed files produces no output.
    - Every `APPROVED-STRING: signup.*` value from `02-COPY-DRAFT.md` was found verbatim in a shipped source file; the check names any key it could not find and exits non-zero rather than passing silently.
    - `npm test`, `npm run typecheck`, and `npm run lint` all exit 0.
    - `git status --porcelain` shows no source file changed by this task; it runs gates only.
    - The plan SUMMARY records all four gate commands verbatim under a reusable heading.
    - The human-check items are recorded for end-of-phase UAT harvest.
  </acceptance_criteria>
  <done>The four gates the rest of the phase depends on are proven to work, and the signup surface passes all of them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signup domain (lootlistplus.com) to marketing domain (getlootlist.com) | The secondary link crosses origins. A wrong or relative href sends a converting officer to a dead end. |
| approved artifact to committed source | Anything committed here auto-deploys from main to production. A string that drifts between approval and commit ships unreviewed. |
| user to Discord OAuth | Unchanged by this plan, but the plan edits the file that hosts it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering | Copy drift between the approved artifact and the committed source | high | mitigate | Gate 2 checks every `APPROVED-STRING: signup.*` value appears verbatim in a shipped file and names any key it cannot find. The task precondition halts if the sign-off line is absent. |
| T-02-11 | Spoofing | Outbound cross-domain anchor in `LoginPage.tsx` | low | mitigate | The href is a fixed literal to a first-party domain recorded as an approved string, not built from any input. It is a same-owner navigation, so no `rel` hardening is required; the test asserts the exact value so it cannot be swapped silently. |
| T-02-12 | Elevation of Privilege | Accidental removal of `robots: { index: false }` from `app/page.tsx` while editing its Metadata block | medium | mitigate | The action names `robots` as untouched, and an acceptance criterion greps for `index: false`. Indexing an authenticated login surface would split the site's own ranking signal. |
| T-02-13 | Denial of Service | Breaking `handleDiscordLogin` or the invite-code fetch while editing surrounding JSX | medium | mitigate | The action scopes the edit to four text nodes and one href. An acceptance criterion requires the diff to show no change to `handleDiscordLogin`, the Alert strings, or the invite logic, and the human-check confirms OAuth still starts. |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no packages. 02-RESEARCH.md marks the Package Legitimacy Audit not applicable; Vitest and Testing Library are already project dependencies. |
</threat_model>

<verification>
- `npx vitest run app/components/__tests__/LoginPage.test.tsx` passes.
- `npm test`, `npm run typecheck`, `npm run lint` all exit 0.
- Diff-scoped em-dash grep excluding `.planning/` produces no output.
- Every `APPROVED-STRING: signup.*` value appears verbatim in `app/components/LoginPage.tsx` or `app/page.tsx`.
- `grep -c 'https://www.getlootlist.com/#how-it-works' app/components/LoginPage.tsx` returns 1.
- `grep -q 'index: false' app/page.tsx` succeeds.
</verification>

<success_criteria>
The signup surface says only what the user approved, its secondary link works across domains, a test pins every string, and the gate battery the rest of the phase reuses is proven on real files.
</success_criteria>

<artifacts_this_phase_produces>
Introduced by this plan:

- `app/components/__tests__/LoginPage.test.tsx` (new file) exporting no symbols, containing the render assertions for every approved signup string and the absolute secondary href
- Modified `app/components/LoginPage.tsx`: H1 text node, body text node, primary Button label, secondary anchor text node, secondary anchor `href` (now `https://www.getlootlist.com/#how-it-works`)
- Modified `app/page.tsx`: `metadata.title`, `metadata.description`
- Reusable gate commands recorded in `02-03-SUMMARY.md`: the diff-scoped em-dash grep, the `APPROVED-STRING` parity loop, and the `npm test` plus `npm run typecheck` plus `npm run lint` battery
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-checkable-conversion-copy/02-03-SUMMARY.md` when done
</output>
