---
phase: 02-checkable-conversion-copy
plan: 06
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - app/about/page.tsx
  - app/terms/page.tsx
  - app/privacy/page.tsx
  - app/components/landing/LandingHero.tsx
  - public/site.webmanifest
  - README.md
autonomous: true
requirements: [PROOF-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The About, Terms, and Privacy surfaces carry the broadened positioning wording, so the legal definition of the service matches how the product describes itself everywhere else (D-12)"
    - "The marketing hero body copy carries the broadened positioning wording while the hero H1 is untouched (D-10, D-12)"
    - "The web app manifest description no longer asserts an unverifiable superlative (PROOF-02, D-15)"
    - "The README opening description matches the approved definitive product description used in the root layout metadata and JSON-LD, so the checklist named in that file's inline comment holds (D-12)"
    - "Every remaining occurrence of the narrower positioning phrase in each touched file equals the count the user approved in that file's RESIDUAL-ALLOWANCE line"
    - "Every shipped string byte-matches its APPROVED-STRING value in 02-COPY-DRAFT.md (D-16)"
    - "No positioning string on any touched surface claims Retail or current-expansion support (D-13)"
    - "No em dash appears in any source file changed by this plan"
    - "The expansion list on /about still names the same five expansions the audit fact-checked, so the replacement stat card on the homepage stays confirmable against it (D-06, PROOF-02)"
  artifacts:
    - path: "app/about/page.tsx"
      provides: "About page Metadata title and description carrying the broadened positioning"
      contains: "Mists of Pandaria"
    - path: "app/terms/page.tsx"
      provides: "Terms Metadata description and the body sentence defining what the service is"
      contains: "loot management"
    - path: "app/privacy/page.tsx"
      provides: "Privacy body sentence defining the service"
      contains: "loot management"
    - path: "app/components/landing/LandingHero.tsx"
      provides: "Hero body copy carrying the broadened positioning, with the H1 unchanged"
      contains: "LootList+"
    - path: "public/site.webmanifest"
      provides: "Installable-app description with the superlative removed"
      contains: "description"
    - path: "README.md"
      provides: "Opening product description matching the root layout's definitive description"
      contains: "LootList+"
  key_links:
    - from: "README.md"
      to: "app/layout.tsx"
      via: "the inline comment above the root metadata description names the README as one of the surfaces that must carry an identical product description"
      pattern: "transparent loot-management system"
    - from: "app/about/page.tsx"
      to: "app/components/landing/LandingValueProps.tsx"
      via: "the /about expansion list is the source a visitor checks the homepage replacement stat card against"
      pattern: "Mists of Pandaria"
  flagged_assumptions:
    - "PLANNER ASSUMPTION (legal surfaces): `app/terms/page.tsx` and `app/privacy/page.tsx` define the service in body prose, so broadening that wording changes what the legal text says the service covers. Planner assumption: broadening from the narrower phrase to the general product name is descriptive rather than substantive, because Classic is World of Warcraft and no new capability is claimed (D-13). Flagged, not resolved. If the developer wants a lawyer to look at the two legal surfaces before they ship, that is a separate gate this plan does not impose."
    - "PLANNER ASSUMPTION (definitive description): the inline comment in `app/layout.tsx` declares one product description that must be identical across the root metadata, the homepage, /about, the README, JSON-LD, and directory listings. Planner assumption: `public/site.webmanifest` counts as a directory listing under that comment and should carry the same description once its superlative is removed. Flagged, not resolved; the approved draft decides."
---

<!-- planner-discipline-allow: WoW Classic -->
<!-- planner-discipline-allow: World of Warcraft Classic -->

<objective>
Finish the repositioning on the surfaces a visitor reads as prose rather than as a search snippet, and delete the last unverifiable superlative from the app's own manifest.

**As an** officer reading the About page, the Terms, or the hero, **I want to** see the product describe its own scope the same way everywhere, **so that** I do not have to wonder which description is the real one.

Purpose: D-12 rates a half-applied broadening as worse than either consistent state, and the root layout's own inline comment names /about and the README as surfaces that must carry an identical product description. This plan closes that set. It also removes the superlative from `public/site.webmanifest`, which is the last place on the in-repo surface that calls the product the ultimate anything.

Output: six prose and config surfaces carrying the approved wording, with every residual occurrence accounted for.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/loot-list-plus/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/loot-list-plus/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md
@.planning/phases/02-checkable-conversion-copy/02-COPY-AUDIT.md
@.planning/phases/02-checkable-conversion-copy/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Broaden the positioning on the About, Terms, and Privacy surfaces</name>
  <precondition>`.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md` contains a line matching `^SIGN-OFF: APPROVED ` and a `RESIDUAL-ALLOWANCE` line for each of `app/about/page.tsx`, `app/terms/page.tsx`, and `app/privacy/page.tsx`.</precondition>
  <reversibility rating="costly">D-12 spans 13 files and triggers the standing full-surface sweep rule; a partially applied broadening costs a revisit of every surface. The user chose this in CONTEXT.md, so no gate is re-asked.</reversibility>
  <files>app/about/page.tsx, app/terms/page.tsx, app/privacy/page.tsx</files>
  <read_first>
    - `app/about/page.tsx`. Two occurrences of the narrower phrase, both in the Metadata block at lines 9 (`title`) and 11 (`description`). Lines 87 to 88 carry the visible five-expansion list, which stays exactly as it is because the homepage stat card is checked against it.
    - `app/terms/page.tsx`. Two occurrences: the Metadata `description` at line 8 and the body sentence at line 40 that defines what the service is.
    - `app/privacy/page.tsx`. One occurrence, in the body prose at line 33.
    - `.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md`, section E's disposition rows for these three files and every `APPROVED-STRING: repos.app/about/page.tsx.*`, `repos.app/terms/page.tsx.*`, and `repos.app/privacy/page.tsx.*` line
  </read_first>
  <action>
    Replace each occurrence with the matching approved value, one occurrence at a time, taking the text verbatim from its `APPROVED-STRING: repos.<path>.<line>` line. An occurrence whose approved disposition is keep stays byte-identical.

    On `/about`, both occurrences are metadata. The visible H1 and body prose on that page do not carry the narrower phrase today, so nothing in the page body changes here except what the approved table names. Do not touch the five-expansion list at lines 87 to 88: it is the source a visitor uses to confirm the homepage replacement stat card, and the audit fact-checked it as is.

    On `/terms` and `/privacy`, one occurrence in each is body prose that defines the service in a legal document, not a marketing snippet. Change exactly the phrase the approved table names and nothing else in the surrounding sentence, so the diff on a legal surface stays reviewable at a glance.

    Change no other content, no component structure, and no styling in any of the three files.
  </action>
  <verify>
    <automated>npm run typecheck && for F in app/about/page.tsx app/terms/page.tsx app/privacy/page.tsx; do KEY=$(printf '%s' "$F" | sed 's/[].[^$\\*\/()]/\\&/g'); N=$(grep -oE "^RESIDUAL-ALLOWANCE: ${KEY} = [0-9]+$" .planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md | grep -oE '[0-9]+$'); test -n "$N" || { echo "no RESIDUAL-ALLOWANCE recorded for $F"; exit 1; }; A=$(grep -cE 'WoW Classic|World of Warcraft Classic' "$F" || true); test "$A" = "$N" || { echo "$F has $A residual occurrences, approved allowance is $N"; exit 1; }; done; grep -q 'Mists of Pandaria' app/about/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - For each of the three files, the residual occurrence count equals that file's `RESIDUAL-ALLOWANCE` number, and a missing allowance line fails the check by name.
    - Every `APPROVED-STRING: repos.app/about/page.tsx.*`, `repos.app/terms/page.tsx.*`, and `repos.app/privacy/page.tsx.*` value appears verbatim in the corresponding file.
    - `app/about/page.tsx` still contains the five-expansion list naming Mists of Pandaria, byte-identical to its pre-plan state.
    - The `git diff` for `app/terms/page.tsx` and `app/privacy/page.tsx` shows only the named phrase changing inside each affected sentence, with no other legal wording altered.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>About, Terms, and Privacy describe the product's scope the same way the rest of the site does, with the expansion list intact.</done>
</task>

<task type="auto">
  <name>Task 2: Broaden the hero body copy, strip the manifest superlative, align the README, and run the gate battery</name>
  <precondition>`.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md` contains a `RESIDUAL-ALLOWANCE` line for each of `app/components/landing/LandingHero.tsx`, `public/site.webmanifest`, and `README.md`.</precondition>
  <files>app/components/landing/LandingHero.tsx, public/site.webmanifest, README.md</files>
  <read_first>
    - `app/components/landing/LandingHero.tsx`. One occurrence, in the hero body paragraph at line 135. The hero H1 in this file is the sacred one D-10 protects and must not change. This file also holds a live guild and raider counter fetched from `/api/guild-count`, which is untouched by this phase.
    - `public/site.webmanifest` line 4, whose `description` field carries both the narrower positioning phrase and the superlative the audit flagged.
    - `README.md` line 3, the opening product description.
    - `app/layout.tsx` lines 38 to 39 and its inline comment declaring the one definitive product description, plus the value plan 02-05 approved for it
    - `.planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md`, section C for the manifest superlative disposition, section E for these three files, and `.planning/phases/02-checkable-conversion-copy/02-03-SUMMARY.md` for the four reusable gate commands
  </read_first>
  <action>
    In `app/components/landing/LandingHero.tsx`, replace only the phrase named in the approved disposition table inside the body paragraph at line 135. Do not touch the H1, the shimmer span, any animation prop, the counter fetch, or any `className`. If the approved table also changed other words in that paragraph, apply exactly the approved sentence and nothing beyond it.

    In `public/site.webmanifest`, replace the `description` value with its approved string. That value must lose both the narrower positioning phrase and the superlative the audit flagged, per the approved section C disposition. Keep the file valid JSON and change no other key.

    In `README.md`, replace the opening description with its approved string. Then confirm it is character-for-character the same sentence plan 02-05 shipped into `app/layout.tsx`'s `metadata.description`, because the inline comment there names the README as one of the surfaces that must carry an identical product description. If the two differ, do not silently pick one: stop and surface the divergence with both strings quoted.

    This is the in-repo half of the sweep only. Per D-14 the Discord server description, the GitHub repository description field, and any old external posts are founder owned and are never edited by an executor. They are covered by the handoff checklist in section F of the approved draft; confirm that checklist exists and name it in the SUMMARY so the user knows what is still outstanding.

    Then run the gate battery from `02-03-SUMMARY.md`: the diff-scoped em-dash grep excluding the planning directory, the `APPROVED-STRING` parity loop scoped to this plan's six files, and `npm test` plus `npm run typecheck` plus `npm run lint`.
  </action>
  <verify>
    <automated>npm run typecheck && node -e "JSON.parse(require('fs').readFileSync('public/site.webmanifest','utf8'))" && for F in app/components/landing/LandingHero.tsx public/site.webmanifest README.md; do KEY=$(printf '%s' "$F" | sed 's/[].[^$\\*\/()]/\\&/g'); N=$(grep -oE "^RESIDUAL-ALLOWANCE: ${KEY} = [0-9]+$" .planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md | grep -oE '[0-9]+$'); test -n "$N" || { echo "no RESIDUAL-ALLOWANCE recorded for $F"; exit 1; }; A=$(grep -cE 'WoW Classic|World of Warcraft Classic' "$F" || true); test "$A" = "$N" || { echo "$F has $A residual occurrences, approved allowance is $N"; exit 1; }; done; test "$(grep -ci 'the ultimate' public/site.webmanifest)" = "0"; BASE=$(git rev-parse HEAD~1); CHANGED=$(git diff --name-only "$BASE"...HEAD | grep -v '^\.planning/' || true); if [ -n "$CHANGED" ]; then echo "$CHANGED" | xargs -r grep -n '—' && { echo "em dash found in changed source"; exit 1; }; fi; grep -q '^## F\. External handoff checklist' .planning/phases/02-checkable-conversion-copy/02-COPY-DRAFT.md && npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `public/site.webmanifest` parses as valid JSON and `grep -ci 'the ultimate' public/site.webmanifest` returns 0.
    - For each of the three files, the residual occurrence count equals that file's `RESIDUAL-ALLOWANCE` number.
    - The `README.md` opening description is character-for-character identical to `metadata.description` in `app/layout.tsx`; any divergence is surfaced with both strings quoted rather than resolved unilaterally.
    - `git diff app/components/landing/LandingHero.tsx` shows no change to the H1, to the shimmer span, to any animation prop, to the `/api/guild-count` fetch, or to any `className`.
    - Section F of the approved draft exists and is named in the SUMMARY as the outstanding founder-owned work; no Discord, GitHub repository description, or external post was edited.
    - Every `APPROVED-STRING` value scoped to this plan's six files is found verbatim; the check names any key it cannot find and exits non-zero.
    - The em-dash grep over the diff-scoped, non-planning changed files produces no output.
    - `npm test`, `npm run typecheck`, and `npm run lint` all exit 0.
  </acceptance_criteria>
  <done>The hero body, the manifest, and the README all carry the approved wording, the manifest superlative is gone, the definitive description agrees across surfaces, and the six files clear the gate battery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| marketing wording to legal text | Terms and Privacy define the service in prose that has contractual weight; broadening it is not purely cosmetic. |
| authored description to installed-app surfaces and directory listings | `public/site.webmanifest` is what an installed shortcut and some listings display. |
| approved artifact to committed source | Commits land on main and auto-deploy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-17 | Spoofing | A broadened positioning string read as a Retail compatibility claim, especially in the Terms body sentence defining the service | high | mitigate | D-13 bans any Retail claim, the wording was reviewed by a human at the D-16 sign-off, and the acceptance criteria hold the diff on the two legal surfaces to the named phrase only so a reviewer can see the whole change at a glance. |
| T-02-20 | Tampering | Invalid JSON written into `public/site.webmanifest` | medium | mitigate | The verify gate parses the file with `JSON.parse` and fails on a syntax error. A broken manifest degrades the installable-app experience silently in the browser. |
| T-02-21 | Repudiation | Two divergent definitive product descriptions shipping at once, one in the root metadata and one in the README | medium | mitigate | The task compares the two strings character for character and halts with both quoted rather than picking one, honouring the checklist in the root layout's own inline comment. |
| T-02-22 | Elevation of Privilege | Editing a founder-owned external surface (Discord, GitHub repository description, old posts) | medium | mitigate | D-14 forbids it. The task ships only in-repo files and requires the section F handoff checklist to exist and be named in the SUMMARY. |
| T-02-10 | Tampering | Copy drift between the approved artifact and the committed source | high | mitigate | The parity loop covers all six files and names any approved key it cannot find. Both tasks carry preconditions on the sign-off and allowance lines. |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no packages. 02-RESEARCH.md marks the Package Legitimacy Audit not applicable. |
</threat_model>

<verification>
- For each of the six files, the residual occurrence count equals that file's approved `RESIDUAL-ALLOWANCE` number.
- `public/site.webmanifest` parses as JSON and contains no superlative.
- `README.md` and `app/layout.tsx` carry the identical definitive product description.
- `app/components/landing/LandingHero.tsx` H1 is unchanged in the diff.
- `app/about/page.tsx` still lists the same five expansions.
- Diff-scoped em-dash grep excluding `.planning/` produces no output.
- `npm test`, `npm run typecheck`, `npm run lint` all exit 0.
</verification>

<success_criteria>
Every in-repo prose and config surface describes the product's scope the same way, no surface calls it the ultimate anything, and the only repositioning work left is the founder-owned external checklist.
</success_criteria>

<artifacts_this_phase_produces>
Modified by this plan (no new symbols or files):

- `app/about/page.tsx`: `metadata.title`, `metadata.description`
- `app/terms/page.tsx`: `metadata.description` and the body sentence defining the service
- `app/privacy/page.tsx`: the body sentence defining the service
- `app/components/landing/LandingHero.tsx`: the hero body paragraph text node
- `public/site.webmanifest`: the `description` key
- `README.md`: the opening product description line
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-checkable-conversion-copy/02-06-SUMMARY.md` when done
</output>
