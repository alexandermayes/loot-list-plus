---
phase: 01-measurement-baseline-ai-answer-log
plan: 04
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - scripts/analytics/ai-answer-log.csv
autonomous: false
requirements: [MEAS-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "scripts/analytics/ai-answer-log.csv contains at least one complete run: every one of the 18 prompt-surface cells for a single run date, six prompts against each of the three surfaces locked by D-01"
    - "Every recorded cell originates from a run the user actually performed in a clean non-personalised session and reported back, per D-02"
    - "Cells where LootList+ did not appear are recorded, not omitted, with factually_correct set to n/a and cited_url empty"
    - "Competing sources that outrank LootList+ are recorded rather than softened or dropped"
    - "Every recorded row passes validate_row, so no invalid surface, prompt id, date, or verdict enters the committed log"
    - "Rows are written through the appender rather than by hand editing the CSV, so a competing-sources value containing commas cannot corrupt columns"
    - "The recorded run establishes the week-1 comparison point that the Phase 6 review reads"
  artifacts:
    - path: "scripts/analytics/ai-answer-log.csv"
      provides: "The week-1 run: 18 recorded prompt-surface cells"
      contains: "google-ai-overviews"
      min_lines: 19
  key_links:
    - from: "scripts/analytics/ai-answer-log.csv"
      to: "scripts/analytics/log-ai-answer.py"
      via: "every row is appended through the validating appender CLI, never by editing the CSV"
      pattern: "log-ai-answer.py"
    - from: "scripts/analytics/ai-answer-log.csv"
      to: "scripts/analytics/RUNBOOK.md"
      via: "the recorded surfaces and prompt ids are exactly the ones the runbook instructs, so the run is reproducible next week"
      pattern: "chatgpt|google-ai-overviews|claude"
  prohibitions:
    - statement: "MUST NOT fabricate, infer, extrapolate, or approximate any AI-answer cell. Every logged cell must originate from a run a human actually performed and reported, in a clean non-personalised session as the runbook specifies. An unrun cell stays unrecorded."
      status: resolved
      verification: judgment
    - statement: "MUST NOT omit, soften, or cherry-pick unfavourable cells. A recorded run includes every prompt-surface cell, including non-appearances, factually wrong answers, and competing sources that outrank LootList+."
      status: resolved
      verification: judgment
---

<objective>
Record the first complete test-set run into the results log, satisfying success criterion 4 and giving Phase 6 its week-1 comparison point.

Purpose: the instrument built in plan 01-02 is worthless until it has actually collected once. D-02 fixes how: the user runs the six prompts against the three surfaces in clean non-personalised sessions and reports the results, and the executor records them. Nothing here may be driven through the user's authenticated browser and nothing may be invented.
Output: 18 recorded rows in `scripts/analytics/ai-answer-log.csv`, committed.

Note on shape: the end-to-end tracer for this stack is plan 01-02 task 1, which proved one cell can travel from the appender CLI into the log and back out intact. This plan is the real collection.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/loot-list-plus/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/loot-list-plus/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-measurement-baseline-ai-answer-log/01-CONTEXT.md
@.planning/phases/01-measurement-baseline-ai-answer-log/01-02-SUMMARY.md
@scripts/analytics/RUNBOOK.md
</context>

## Artifacts this phase produces

New symbols and paths created by Phase 1. A drift check should treat every entry as newly created by this phase rather than as an unresolved reference.

**Created by this plan (01-04):** the first 18 data rows in `scripts/analytics/ai-answer-log.csv`. No new file and no new symbol.

**Consumed from plan 01-02, not created here:** `scripts/analytics/RUNBOOK.md`; `scripts/analytics/log-ai-answer.py` with its `HEADER`, `SURFACES`, `PROMPT_IDS`, `PROMPTS`, `APPEARED_VALUES`, `CORRECT_VALUES`, `validate_row()`, `append_row()`, and `read_rows()`, and its `--date`, `--surface`, `--prompt-id`, `--appeared`, `--correct`, `--cited-url`, `--competing-sources`, `--notes`, and `--log` flags; `scripts/analytics/test_ai_answer_log.py`.

**Created by sibling plans:** 01-01 creates the GSC modules and CLI flags; 01-03 creates `scripts/analytics/exports/` and its README; 01-05 replaces the partial cohort export.

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Human-run test set, 18 prompt-surface cells reported back (D-02)</name>
  <read_first>
    - scripts/analytics/RUNBOOK.md (the procedure the user follows; read it before presenting this checkpoint so the inline summary matches the real per-surface steps and does not contradict them)
    - scripts/analytics/log-ai-answer.py (the vocabularies the reported values must map onto)
    - .planning/phases/01-measurement-baseline-ai-answer-log/01-CONTEXT.md (D-01 for the three surfaces, D-02 for why this is a human checkpoint rather than automation)
  </read_first>
  <action>Run the six fixed prompts against the three AI surfaces in clean, non-personalised sessions and report the 18 results</action>
  <instructions>
    Everything that could be automated already is: `scripts/analytics/RUNBOOK.md` is the procedure, `scripts/analytics/log-ai-answer.py` is the validating appender, and `scripts/analytics/ai-answer-log.csv` is waiting with its header. What remains cannot be automated. These answers are UI rendered and personalisation sensitive, and the success criterion requires a clean non-personalised session, so per D-02 you run them and I record what you report. I will not drive your browser and I will not invent a single cell.

    Follow `scripts/analytics/RUNBOOK.md` exactly. In short: open a fresh private or incognito window per surface, stay logged out (or use a temporary chat with memory off on ChatGPT), and paste each prompt unchanged.

    The 18 cells, in order:
      chatgpt: P1, P2, P3, P4, P5, P6
      google-ai-overviews: P1, P2, P3, P4, P5, P6
      claude: P1, P2, P3, P4, P5, P6

    The six prompts are listed verbatim in the runbook. P3 contains a typographic apostrophe; copy it from the runbook rather than retyping it.

    For each of the 18 cells, report one line in this format, pipe separated:

      surface | prompt_id | appeared | correct | cited_url | competing_sources | notes

    where appeared is yes or no; correct is yes, no, partial, or n/a (use n/a whenever appeared is no); cited_url is the LootList+ URL the answer cited, left empty if none and required empty when appeared is no; competing_sources is a semicolon separated list of the other products or sites the answer named, empty if none; and notes is free text, optional.

    Report every cell, including the ones where LootList+ did not appear and the ones where the answer was wrong. Those are the most valuable rows in the log: the whole point of the week-4 review is comparing them against a later run. Please do not paste account identifiers, session URLs, or anything from your own browsing history into these fields; this file is committed to a public repository.

    Also tell me the run date in ISO form (the date you actually ran them).
  </instructions>
  <verification>Once you report the lines, I will append each one through `log-ai-answer.py`, then verify that all 18 prompt-surface pairs are present for your run date and that every row passes the appender's validation. If any of the 18 lines is missing I will ask for it rather than filling it in.</verification>
  <acceptance_criteria>
    - A run date in ISO form has been reported by the user
    - One result line per cell has been reported, in the pipe separated format, for cells the user actually ran
    - Every reported surface value maps onto `chatgpt`, `google-ai-overviews`, or `claude`, and every prompt id onto `P1` through `P6`
    - No cell value in the executor's possession originates from anything other than the user's report; nothing was observed, inferred, or generated by the executor
    - Any cell the user could not run is named explicitly so it can be left unrecorded rather than guessed
  </acceptance_criteria>
  <resume-signal>Paste the run date and the 18 result lines. If you could not reach a surface at all, say which and why, and I will leave those cells unrecorded rather than guessing.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Append the reported run to the log and prove the run is complete</name>
  <precondition>scripts/analytics/log-ai-answer.py and scripts/analytics/ai-answer-log.csv exist from plan 01-02, and the user has reported a run date plus one result line per cell in the preceding checkpoint. Do not proceed on a partial report by inventing the missing cells.</precondition>
  <files>scripts/analytics/ai-answer-log.csv</files>
  <read_first>
    - scripts/analytics/log-ai-answer.py (the appender being invoked; read its argparse flags and validate_row rules so every reported line maps onto real flags and passes validation on the first attempt)
    - scripts/analytics/ai-answer-log.csv (the file being appended to; confirm it currently holds only its header before writing)
    - scripts/analytics/RUNBOOK.md (the surface slugs and column vocabularies the report must be mapped onto)
    - .planning/phases/01-measurement-baseline-ai-answer-log/01-CONTEXT.md (D-02, which forbids fabricating or approximating any result)
  </read_first>
  <action>
    Transcribe each reported line into one `python3 scripts/analytics/log-ai-answer.py` invocation, run from the repository root, using the reported run date for `--date` and mapping the pipe separated fields onto `--surface`, `--prompt-id`, `--appeared`, `--correct`, `--cited-url`, `--competing-sources`, and `--notes`. Transcribe values exactly as reported; do not tidy, summarise, reorder, or normalise a competing-sources list, and do not upgrade a reported "partial" to "yes" or soften a "no".

    Use the appender for every row. Never open `scripts/analytics/ai-answer-log.csv` in an editor and never write a line by joining fields on a comma; a cited URL with a query string or a competing-sources list will corrupt columns if hand written, which is exactly the failure the appender exists to prevent.

    If the appender rejects a row, report the validation message and ask which value is wrong. Do not adjust the reported value to make validation pass. The one exception is a purely mechanical mapping fix, for example writing the surface slug `google-ai-overviews` where the user wrote the display name Google AI Overviews; state that mapping in the summary.

    If the user reported fewer than 18 cells, append exactly the cells they reported, then record the unreached cells as an open item in the plan summary naming the surface and prompt ids. Leave them out of the log rather than filling them with a guess (D-02). A run missing cells does not satisfy success criterion 4 and must be reported as such, not rounded up.

    After appending, print the per-surface and per-verdict tallies so the numbers going into Phase 6 are visible in this plan's summary rather than only inside the CSV.
  </action>
  <verify>
    <automated>python3 -c "
import csv, itertools, sys
rows = list(csv.DictReader(open('scripts/analytics/ai-answer-log.csv', newline='')))
assert rows, 'log has no data rows'
dates = sorted({r['date'] for r in rows})
assert len(dates) == 1, ('expected one run date, got', dates)
run = dates[0]
surfaces = ['chatgpt', 'google-ai-overviews', 'claude']
prompts = ['P1', 'P2', 'P3', 'P4', 'P5', 'P6']
want = set(itertools.product(surfaces, prompts))
have = {(r['ai_surface'], r['prompt_id']) for r in rows if r['date'] == run}
missing = sorted(want - have)
assert not missing, ('missing cells', missing)
extra = sorted(have - want)
assert not extra, ('unexpected surface or prompt id', extra)
print('run', run, 'rows', len(rows), 'all 18 cells present')
"</automated>
    <automated>python3 -c "
import csv, importlib.util, sys
spec = importlib.util.spec_from_file_location('lal', 'scripts/analytics/log-ai-answer.py')
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
rows = list(csv.DictReader(open('scripts/analytics/ai-answer-log.csv', newline='')))
bad = [(i, m.validate_row(r)) for i, r in enumerate(rows, 2) if m.validate_row(r)]
assert not bad, bad
print(len(rows), 'rows all valid')
"</automated>
    <automated>python3 -m unittest discover -s scripts/analytics -p 'test_ai_answer_log.py' -v</automated>
    <human-check>Compare the appended rows against the 18 lines you reported. Confirm every verdict, cited URL, and competing-sources list matches what you actually saw, that nothing was tidied or softened, and that no cell you did not run appears in the file.</human-check>
  </verify>
  <acceptance_criteria>
    - `scripts/analytics/ai-answer-log.csv` has at least 19 lines: the header plus one row per reported cell
    - All 18 prompt-surface pairs are present for a single run date: three surfaces from `SURFACES` crossed with `P1` through `P6`
    - No row carries a surface or prompt id outside those vocabularies
    - Every row returns an empty problem list from `log-ai-answer.validate_row`
    - Every row with `lootlist_appeared` of `no` has `factually_correct` of `n/a` and an empty `cited_url`
    - At least one row exists for each of `chatgpt`, `google-ai-overviews`, and `claude`
    - `python3 -m unittest discover -s scripts/analytics -p 'test_ai_answer_log.py' -v` still exits 0 with real rows present, except for the header-only assertion from plan 01-02, which task 2 must update to assert the header is intact rather than that the file is empty
    - The plan summary reports the per-surface and per-verdict tallies, and names any cell the user could not run
  </acceptance_criteria>
  <done>The log holds one complete run of all 18 prompt-surface cells for a single date, every row validated, every row traceable to something the user actually saw.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user's browser observation to reported text | The only evidence is what the user retypes or pastes; nothing mechanically re-checks it |
| reported text to committed CSV | Free-text values cross into a file committed to a confirmed PUBLIC repository |
| committed log to Phase 6 conclusion | This run is the sole week-1 baseline the review compares against |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Tampering | recording the reported run | high | mitigate | The checkpoint is `gate="blocking-human"`, so it is never auto-approved even under an unattended run; no task may synthesise a cell; a short report is recorded short and flagged as short. The 18-pair completeness gate makes an omission a visible failure rather than a silent one. |
| T-01-10 | Spoofing | a personalised session presented as clean | medium | mitigate | The checkpoint restates the private-window and logged-out requirement inline, so the operator does not have to remember it, and the runbook holds the per-surface steps. Procedural by nature, which is why it is a human checkpoint rather than an automated step (D-02). |
| T-01-07 | Tampering | free-text values written into the CSV | medium | mitigate | Every row is written through `append_row`, which uses `csv.writer`; hand editing is forbidden by the action and would be caught by the validation gate re-reading every row. |
| T-01-09 | Information Disclosure | notes and cited_url committed to a public repository | low | mitigate | The checkpoint instructions explicitly ask the user not to paste account identifiers, session URLs, or personal browsing detail, and the schema has no identity field. |
| T-01-13 | Repudiation | a later reader cannot tell which cells were actually run | medium | mitigate | One row per run cell, append only, with the run date on every row, and unreached cells named in the plan summary instead of being silently absent. |
| T-01-SC | Tampering | npm / pip / cargo installs | high | accept | No package-manager install occurs. Stdlib-only Python per repo convention; RESEARCH.md's Package Legitimacy Audit records the gate as not applicable, so there is no `[ASSUMED]` or `[SUS]` package to verify. |
</threat_model>

<verification>
- All 18 prompt-surface pairs present for one run date in `scripts/analytics/ai-answer-log.csv`
- Every row passes `log-ai-answer.validate_row`
- Every non-appearance row has `n/a` for factual correctness and an empty cited URL
- `python3 -m unittest discover -s scripts/analytics -p 'test_ai_answer_log.py' -v` exits 0
- The user confirms the appended rows match what they reported, with nothing tidied, softened, or invented
</verification>

<success_criteria>
- Success criterion 4 is satisfied: at least one complete test-set run is recorded, establishing the week-1 comparison point for Phase 6
- No cell in the log was produced by anything other than a run the user performed and reported (D-02)
- The log is comparable next week because the surfaces, prompt ids, and vocabularies are the ones the runbook fixes
</success_criteria>

<output>
Create `.planning/phases/01-measurement-baseline-ai-answer-log/01-04-SUMMARY.md` when done
</output>
