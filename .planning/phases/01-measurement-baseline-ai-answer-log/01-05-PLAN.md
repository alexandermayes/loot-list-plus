---
phase: 01-measurement-baseline-ai-answer-log
plan: 05
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - scripts/analytics/exports/gsc-baseline-cohort-query-2026-08-24_2026-08-30.csv
  - scripts/analytics/exports/README.md
  - .planning/STATE.md
autonomous: false
requirements: [MEAS-01]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A committed export covers the complete 2026-08-24 to 2026-08-30 baseline cohort with Search Console final data for every day of that window"
    - "The complete cohort export filename carries no partial marker, because the true final-data end date now equals the requested end date"
    - "The superseded partial cohort export is removed from the repository so exactly one cohort file exists and nobody can read the stale one by mistake"
    - "Every row of the replacement export carries exactly one cluster value from the closed set brand, competitor, problem, expansion, unclustered, and the bucket counts sum to the row count"
    - "scripts/analytics/exports/README.md records the replacement, its row count, and the date the complete pull was taken"
    - "The STATE.md open item is closed with the same convention already used for the resolved OAuth blocker, not deleted"
    - "This plan cannot run before 2026-09-02, and it halts visibly rather than pulling an incomplete window and calling it complete"
  artifacts:
    - path: "scripts/analytics/exports/gsc-baseline-cohort-query-2026-08-24_2026-08-30.csv"
      provides: "The complete Aug 24 to 30 baseline cohort, clustered, with no partial marker"
      contains: "query,clicks,impressions,ctr,position,cluster"
    - path: "scripts/analytics/exports/README.md"
      provides: "Provenance updated to the complete cohort export"
      contains: "2026-08-30"
  key_links:
    - from: "scripts/analytics/exports/README.md"
      to: "scripts/analytics/exports/gsc-baseline-cohort-query-2026-08-24_2026-08-30.csv"
      via: "the provenance row names the replacement file, its window, its true final-data end date, and its row count"
      pattern: "gsc-baseline-cohort-query-2026-08-24_2026-08-30"
    - from: ".planning/STATE.md"
      to: "scripts/analytics/exports/README.md"
      via: "the closed open item points at the README provenance row as the evidence the re-pull happened"
      pattern: "exports/README.md"
  prohibitions:
    - statement: "MUST NOT present an incomplete Search Console window as if it were complete, and MUST NOT fill the missing days by extrapolation, modelling, averaging, or substitution from any other data source. If the complete window is still unavailable, the partial file and its label stay exactly as they are."
      status: resolved
      verification: judgment
---

<objective>
Close the one deliberate gap D-03 left open: re-pull the Aug 24 to 30 baseline cohort once Search Console has finalised the whole window, replace the partial export, and close the STATE.md open item.

Purpose: success criterion 1 asks for an export that covers the Aug 24 to 30 cohort. Plan 01-03 shipped it labelled partial because Search Console final data lags two to three days and the phase must not be held hostage to that lag. This plan is the dated follow-up D-03 requires. It is deliberately gated on the calendar: run before 2026-09-02 and it halts, because the only way to "succeed" early would be to pretend an incomplete week is complete.
Output: one complete cohort export, an updated provenance README, and a closed open item.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/loot-list-plus/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/loot-list-plus/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-measurement-baseline-ai-answer-log/01-CONTEXT.md
@.planning/phases/01-measurement-baseline-ai-answer-log/01-03-SUMMARY.md
@scripts/analytics/exports/README.md
</context>

## Artifacts this phase produces

New symbols and paths created by Phase 1. A drift check should treat every entry as newly created by this phase rather than as an unresolved reference.

**Created by this plan (01-05):** `scripts/analytics/exports/gsc-baseline-cohort-query-2026-08-24_2026-08-30.csv` (the unmarked complete export, replacing the partial-marked file plan 01-03 committed). No new symbol.

**Consumed from earlier plans, not created here:** `pull-gsc.py`'s `--start`, `--end`, `--dimension`, and `--csv` flags and `gsc_export.partial_suffix_path()` from plan 01-01; `scripts/analytics/exports/README.md` and the STATE.md open item from plan 01-03.

<tasks>

<task type="auto">
  <name>Task 1: Re-pull the complete Aug 24 to 30 cohort and replace the partial export</name>
  <precondition>The current date is 2026-09-02 or later, so Search Console has finalised data through 2026-08-30 (final data lags two to three days). Check with a read-only date command. If the current date is earlier, stop and surface the block; do not pull a shorter window and do not adjust the requested end date.</precondition>
  <files>scripts/analytics/exports/gsc-baseline-cohort-query-2026-08-24_2026-08-30.csv</files>
  <read_first>
    - scripts/analytics/exports/README.md (the provenance table written by plan 01-03; read the cohort row to learn the exact partial filename currently committed and the true final-data end date recorded then)
    - scripts/analytics/pull-gsc.py (as extended in plan 01-01; confirm the flag names and that the last stdout line begins with the written-path prefix)
    - scripts/analytics/gsc_export.py (partial_suffix_path, so you can tell from the returned filename alone whether the window came back complete)
    - .planning/phases/01-measurement-baseline-ai-answer-log/01-CONTEXT.md (D-03, which requires this re-pull and forbids approximating the missing days)
  </read_first>
  <action>
    Assert the precondition first with a read-only date check, then re-pull the cohort from the repository root with start 2026-08-24, end 2026-08-30, the query dimension, and the bare output filename `gsc-baseline-cohort-query-2026-08-24_2026-08-30.csv`, which resolves under `scripts/analytics/exports/`.

    Read the last stdout line, which begins with `wrote: `, to learn the filename that actually landed. If that name still carries a partial marker, the window is still not final: stop, leave both the previously committed partial file and its README row exactly as they are, and report the block with the true final-data end date the script reported. Do not shorten the requested window, do not rename the file to look complete, and do not estimate, average, or substitute the missing days from any other source (D-03).

    When the returned name carries no marker, the window is complete. Remove the superseded partial file from the repository with `git rm` so exactly one cohort export remains; a second stale file is the only way a reader ends up quoting the wrong numbers. Confirm afterwards that the cohort glob matches exactly one path.

    Record, for the next task, the replacement file's row count and the date you took the complete pull.
  </action>
  <verify>
    <automated>test "$(ls scripts/analytics/exports/gsc-baseline-cohort-query-2026-08-24_2026-08-30*.csv | wc -l | tr -d ' ')" = "1" && test -f scripts/analytics/exports/gsc-baseline-cohort-query-2026-08-24_2026-08-30.csv && head -1 scripts/analytics/exports/gsc-baseline-cohort-query-2026-08-24_2026-08-30.csv | grep -qx 'query,clicks,impressions,ctr,position,cluster'</automated>
    <automated>python3 -c "
import csv
p = 'scripts/analytics/exports/gsc-baseline-cohort-query-2026-08-24_2026-08-30.csv'
A = {'brand', 'competitor', 'problem', 'expansion', 'unclustered'}
rows = list(csv.DictReader(open(p, newline='')))
assert rows, 'no data rows in ' + p
bad = sorted({r['cluster'] for r in rows} - A)
assert not bad, bad
counts = {}
for r in rows:
    counts[r['cluster']] = counts.get(r['cluster'], 0) + 1
assert sum(counts.values()) == len(rows), (counts, len(rows))
print(p, len(rows), counts)
"</automated>
    <automated>python3 -c "
import subprocess, sys
out = subprocess.run(['git', 'ls-files', 'scripts/analytics/exports'], capture_output=True, text=True, check=True).stdout.split()
cohort = [p for p in out if 'baseline-cohort-query' in p]
assert cohort == ['scripts/analytics/exports/gsc-baseline-cohort-query-2026-08-24_2026-08-30.csv'], cohort
print('one tracked cohort export:', cohort[0])
"</automated>
  </verify>
  <acceptance_criteria>
    - The current date at execution time is 2026-09-02 or later; otherwise the task halts and reports the block rather than producing a file
    - Exactly one file matches `scripts/analytics/exports/gsc-baseline-cohort-query-2026-08-24_2026-08-30*.csv`, and its name carries no partial marker
    - `git ls-files scripts/analytics/exports` lists exactly one cohort export path, so the superseded partial file is no longer tracked
    - The replacement file's first line is exactly `query,clicks,impressions,ctr,position,cluster` and it has at least one data row
    - Every `cluster` value in the replacement file is in the closed five-value set and the bucket counts sum to the row count
    - The replacement file's row count is greater than or equal to the partial file's recorded row count, since the window now covers more days; if it is smaller, report it rather than committing silently
  </acceptance_criteria>
  <done>One cohort export exists, it covers all seven days of the baseline week with final data, and no stale partial file remains tracked.</done>
</task>

<task type="auto">
  <name>Task 2: Update the exports provenance and close the STATE.md open item</name>
  <files>scripts/analytics/exports/README.md, .planning/STATE.md</files>
  <read_first>
    - scripts/analytics/exports/README.md (the file being modified; read the existing cohort provenance row and the dated re-pull section so the update replaces them rather than duplicating them)
    - .planning/STATE.md (the file being modified; read the Blockers and Concerns section, and specifically the strikethrough-plus-RESOLVED convention already used for the OAuth item, and follow it)
    - .planning/phases/01-measurement-baseline-ai-answer-log/01-CONTEXT.md (D-03, whose follow-up this closes)
  </read_first>
  <action>
    In `scripts/analytics/exports/README.md`, replace the cohort provenance row with one describing the complete export: the unmarked filename, the query dimension, the requested window 2026-08-24 to 2026-08-30, a true final-data end date of 2026-08-30, the new row count, and the reproducing command. Then rewrite the dated re-pull section into a short completed note stating the date the complete pull was taken and that the superseded partial file was removed. Keep the filename-convention explanation and the partial-marker semantics in place, because future exports will still need them. Do not use typographic long dash characters anywhere in this file.

    In `.planning/STATE.md`, close the partial-cohort open item using the existing convention: strike through the item text and append a RESOLVED note carrying the resolution date, the fact that the complete window was pulled, the new filename, and a pointer to `scripts/analytics/exports/README.md` as the evidence. Do not delete the item; the resolved OAuth blocker above it is the pattern to follow, and the audit trail of what was open and when it closed is the point. Update the Session Continuity lines to reflect this work.
  </action>
  <verify>
    <automated>grep -Fq 'gsc-baseline-cohort-query-2026-08-24_2026-08-30.csv' scripts/analytics/exports/README.md && grep -Fq '2026-08-30' scripts/analytics/exports/README.md && test "$(LC_ALL=C grep -c $'\xe2\x80\x94' scripts/analytics/exports/README.md || true)" = "0"</automated>
    <automated>python3 -c "
s = open('.planning/STATE.md').read()
block = s.split('### Blockers/Concerns')[1].split('## Deferred Items')[0]
assert 'RESOLVED' in block, 'no RESOLVED marker in Blockers/Concerns'
hits = [line for line in block.splitlines() if 'cohort' in line.lower()]
assert hits, 'no cohort line in Blockers/Concerns'
assert any('RESOLVED' in line and '~~' in line for line in hits), hits
assert any('exports/README.md' in line for line in hits), hits
print('closed:', hits[0][:120])
"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/analytics/exports/README.md` cohort provenance row names the unmarked filename and states a true final-data end date of 2026-08-30
    - `scripts/analytics/exports/README.md` no longer instructs a future reader to perform the re-pull, and instead records the date it was completed
    - `scripts/analytics/exports/README.md` still explains the filename convention and the partial-marker semantics for future exports
    - `scripts/analytics/exports/README.md` contains zero occurrences of the typographic long dash code point, checked byte-wise so the check is not self-invalidating
    - The `.planning/STATE.md` cohort open item is struck through and carries a RESOLVED note with a date, the new filename, and a pointer to `exports/README.md`
    - The cohort open item is still present in `.planning/STATE.md` rather than deleted, matching the resolved OAuth blocker's convention
    - The Session Continuity lines in `.planning/STATE.md` describe this re-pull
  </acceptance_criteria>
  <done>The provenance and the project state both say the same thing: the baseline cohort is complete, when it was pulled, and where the evidence lives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env.local` to script process | OAuth client secret and refresh token cross into the process on the re-pull |
| Search Console API response to committed file | Untrusted remote JSON replaces a permanent repository artifact |
| calendar to coverage claim | The only thing standing between "complete" and a false claim is the date gate plus the live coverage query |
| two coverage records (README and STATE.md) | Drift between them is how a partial export gets read as complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | the replacement export's completeness claim | high | mitigate | Two independent gates: a read-only date precondition that halts before 2026-09-02, and the live date-dimension coverage query whose result decides whether `partial_suffix_path` adds a marker. The task may only proceed when the returned filename has no marker; it may never shorten the window or rename the file to look complete. |
| T-01-14 | Repudiation | a stale partial file left beside the complete one | medium | mitigate | The superseded file is removed with `git rm` and an assertion confirms `git ls-files` lists exactly one tracked cohort export, so there is no second file to quote by accident. |
| T-01-15 | Tampering | deleting the STATE.md open item instead of closing it | low | mitigate | Task 2 requires the strikethrough-plus-RESOLVED convention and an assertion checks both the marker and the retained line, so the audit trail of what was open survives. |
| T-01-01 | Information Disclosure | credential handling during the re-pull | high | mitigate | The pull path never prints or writes secret values, unchanged from plan 01-01; `.gitignore` already excludes `.env*` and `.env*.local`. |
| T-01-03 | Information Disclosure | aggregate performance data in a public repository | medium | accept | Unchanged from plan 01-03: aggregate query performance for one property, no player or guild names, on a repository the project intentionally keeps public. RESEARCH.md Assumption A4. |
| T-01-SC | Tampering | npm / pip / cargo installs | high | accept | No package-manager install occurs. Stdlib-only Python per repo convention; RESEARCH.md's Package Legitimacy Audit records the gate as not applicable, so there is no `[ASSUMED]` or `[SUS]` package to verify. |
</threat_model>

<verification>
- The task halts with a visible block when run before 2026-09-02, and only produces a file on or after that date
- Exactly one tracked cohort export exists and its filename carries no partial marker
- The replacement export passes the closed-set and bucket-sum cluster integrity check
- `scripts/analytics/exports/README.md` and `.planning/STATE.md` agree that the cohort is complete, and both name the same file
- The STATE.md open item is closed by strikethrough plus a RESOLVED note, not by deletion
</verification>

<success_criteria>
- Success criterion 1 is fully satisfied with no labelled gap: the committed export covers the whole Aug 24 to 30 baseline cohort plus the prior three months
- D-03's dated follow-up is discharged on the record, with the date it happened captured in two places
- No day of the cohort was ever estimated, averaged, or sourced from anywhere other than the Search Console API
</success_criteria>

<output>
Create `.planning/phases/01-measurement-baseline-ai-answer-log/01-05-SUMMARY.md` when done
</output>
