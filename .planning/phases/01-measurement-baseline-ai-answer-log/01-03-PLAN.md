---
phase: 01-measurement-baseline-ai-answer-log
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/analytics/exports/README.md
  - scripts/analytics/exports/gsc-trend-query-2026-05-24_2026-08-23.csv
  - scripts/analytics/exports/gsc-trend-page-2026-05-24_2026-08-23.csv
  - scripts/analytics/exports/gsc-baseline-cohort-query-2026-08-24_2026-08-30.csv
  - .planning/STATE.md
autonomous: true
requirements: [MEAS-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A committed export covers the three months immediately preceding the baseline cohort, 2026-05-24 to 2026-08-23, at the query dimension with every row clustered"
    - "A committed export covers the 2026-08-24 to 2026-08-30 baseline cohort at the query dimension with every row clustered"
    - "Because Search Console final data lags two to three days, the cohort export is labelled partial in its own filename with the true last date of final data stated, per D-03"
    - "Every committed export row carries exactly one cluster value from the closed set brand, competitor, problem, expansion, unclustered, and the bucket counts sum to the row count"
    - "scripts/analytics/exports/README.md states, per file, the requested window, the true final-data end date, the row count, and the exact command that reproduces it"
    - "No committed export or README line contains a Search Console OAuth client id, client secret, or refresh token value"
    - "STATE.md carries an open item naming the earliest valid re-pull date of 2026-09-02 and the plan that closes it, so the partial export cannot be forgotten"
    - "No missing day of the cohort window is filled by extrapolation, modelling, or substitution from another data source"
  artifacts:
    - path: "scripts/analytics/exports/README.md"
      provides: "Provenance table for every committed export, the naming convention, the partial-label semantics, and the dated re-pull instruction"
      contains: "2026-09-02"
      min_lines: 30
    - path: "scripts/analytics/exports/gsc-trend-query-2026-05-24_2026-08-23.csv"
      provides: "Prior three months of query performance, clustered"
      contains: "query,clicks,impressions,ctr,position,cluster"
    - path: "scripts/analytics/exports/gsc-trend-page-2026-05-24_2026-08-23.csv"
      provides: "Prior three months of page performance, supporting the Phase 6 position 4 to 20 and weak CTR review"
      contains: "page,clicks,impressions,ctr,position"
    - path: ".planning/STATE.md"
      provides: "Open item recording the partial cohort export and its dated re-pull"
      contains: "2026-09-02"
  key_links:
    - from: "scripts/analytics/exports/README.md"
      to: "scripts/analytics/pull-gsc.py"
      via: "each provenance row carries the literal reproducing command, including its --start, --end, --dimension, and --csv values"
      pattern: "pull-gsc.py --start"
    - from: ".planning/STATE.md"
      to: ".planning/phases/01-measurement-baseline-ai-answer-log/01-05-PLAN.md"
      via: "the open item names plan 01-05 as the closer of the dated re-pull"
      pattern: "01-05"
  prohibitions:
    - statement: "MUST NOT present an incomplete Search Console window as if it were complete, and MUST NOT fill the missing days by extrapolation, modelling, averaging, or substitution from any other data source. A partial window is labelled partial with its true last date of final data, or it is not committed."
      status: resolved
      verification: judgment
---

<assumption_delta_decision>
**Noun now primary:** the data source behind the baseline numbers.
**Detector signal:** a pluralisation term ("another") in the phase scope, from CONTEXT.md's line stating that missing days must never be approximated from another source.
**Decision:** `no-change`.
**Rationale:** the phrase is a prohibition against a second source, not the introduction of one. Google Search Console remains the single source of truth for every baseline search number in this milestone; no alternative, fallback, or supplementary provider is added, and none is permitted as a substitute for missing days.
**Accepted debt:** none. This is not an add-alongside.
**Suggested invariant:** the prohibition recorded in this plan's `must_haves.prohibitions` is the invariant in prose form. A committed export whose coverage gap was filled from anywhere other than the Search Console API violates it.
</assumption_delta_decision>

<objective>
Produce and commit the actual MEAS-01 baseline artifacts using the tooling proved end to end in plan 01-01 task 1, label the cohort export honestly against its true data coverage, and record the dated follow-up so the partial state cannot be lost.

Purpose: success criterion 1 asks for a committed export covering the Aug 24 to 30 cohort plus the prior three months. Search Console final data lags two to three days, so on the planning date only Aug 24 and 25 are final. D-03 resolves this: ship the export now, label it partial with the real end date, and carry an explicit dated follow-up rather than holding the phase or inventing the missing days.
Output: three committed CSV exports, a provenance README, and an open item in STATE.md.

Note on shape: the end-to-end tracer for this stack is plan 01-01 task 1, which already proved the whole path from an explicit window to a clustered file on disk. This plan is expansion: real windows, real committed artifacts.
</objective>

<execution_context>
@/Users/alexander.mayes/Code/loot-list-plus/.claude/gsd-core/workflows/execute-plan.md
@/Users/alexander.mayes/Code/loot-list-plus/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-measurement-baseline-ai-answer-log/01-CONTEXT.md
@.planning/phases/01-measurement-baseline-ai-answer-log/01-RESEARCH.md
@.planning/phases/01-measurement-baseline-ai-answer-log/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols and paths created by Phase 1. A drift check should treat every entry as newly created by this phase rather than as an unresolved reference.

**Created by this plan (01-03):**
- `scripts/analytics/exports/` (new directory)
- `scripts/analytics/exports/README.md` (new file)
- `scripts/analytics/exports/gsc-trend-query-2026-05-24_2026-08-23.csv` (new file)
- `scripts/analytics/exports/gsc-trend-page-2026-05-24_2026-08-23.csv` (new file)
- `scripts/analytics/exports/gsc-baseline-cohort-query-2026-08-24_2026-08-30*.csv` (new file; the script appends a partial marker carrying the true final-data end date when the window is not yet complete)
- `.planning/STATE.md` (modified: one new entry under Blockers and Concerns)

**Consumed from plan 01-01, not created here:** `pull-gsc.py`'s `--start`, `--end`, `--dimension`, and `--csv` flags; `gsc_clusters.cluster_query()`; `gsc_export.partial_suffix_path()`, `resolve_export_path()`, `max_date()`, and `export_csv()`.

**Created by sibling plans:** 01-02 creates `RUNBOOK.md`, `ai-answer-log.csv`, `log-ai-answer.py`, and `test_ai_answer_log.py`; 01-04 writes the first 18 log rows; 01-05 replaces the partial cohort export.

<tasks>

<task type="auto">
  <name>Task 1: Pull and commit the three baseline exports with a provenance README</name>
  <precondition>GSC_CLIENT_ID, GSC_CLIENT_SECRET, GSC_REFRESH_TOKEN, and GSC_SITE_URL are present in .env.local (STATE.md records these were restored and a full pull verified on 2026-08-28). Check presence by key name only; never print, echo, or copy the values.</precondition>
  <files>scripts/analytics/exports/README.md, scripts/analytics/exports/gsc-trend-query-2026-05-24_2026-08-23.csv, scripts/analytics/exports/gsc-trend-page-2026-05-24_2026-08-23.csv, scripts/analytics/exports/gsc-baseline-cohort-query-2026-08-24_2026-08-30.csv</files>
  <read_first>
    - scripts/analytics/pull-gsc.py (as extended in plan 01-01; read the argparse block and write_export so the commands use the real flag names, and so you know the script prints the actually written path on its last line)
    - scripts/analytics/gsc_export.py (partial_suffix_path, to know exactly how the marker is inserted and therefore what filename to expect)
    - .planning/phases/01-measurement-baseline-ai-answer-log/01-CONTEXT.md (D-03, which governs the partial label and the dated follow-up)
    - .planning/phases/01-measurement-baseline-ai-answer-log/01-RESEARCH.md (Pitfall 1, the data-lag trap, and Assumption A4 on the public repository)
  </read_first>
  <action>
    Run three real pulls from the repository root and commit their output, per D-03. Do not hand edit any CSV afterwards.

    First, the prior three months at the query dimension. The window is the 92 days immediately preceding the cohort, stated as explicit dates so it can never drift with the run date: start 2026-05-24, end 2026-08-23. Write it to the bare filename `gsc-trend-query-2026-05-24_2026-08-23.csv`, which `resolve_export_path` places under `scripts/analytics/exports/`.

    Second, the same window at the page dimension, to `gsc-trend-page-2026-05-24_2026-08-23.csv`. This file carries no cluster column by design, since clustering applies to query text. It exists because the Phase 6 review names pages with meaningful impressions at positions 4 to 20 and weak CTR, and it should not have to re-derive that from a live pull weeks later.

    Third, the baseline cohort at the query dimension: start 2026-08-24, end 2026-08-30, to `gsc-baseline-cohort-query-2026-08-24_2026-08-30.csv`. The script queries the date dimension for the same window, discovers the last date Search Console reports final data for, and inserts a partial marker carrying that true date into the filename when it is earlier than 2026-08-30. Read the last stdout line, which begins with `wrote: `, to learn the filename that actually landed, and use that exact name everywhere downstream. Do not rename the file, do not strip the marker, and do not create a second unmarked copy.

    Record the true final-data end date the script reported for the cohort window. That reported value, not an assumption about the length of the lag, is what the README and the STATE.md entry must state.

    Then write `scripts/analytics/exports/README.md`:
    - One paragraph on what this directory is: committed, reproducible Search Console exports that the Phase 6 week-4 review reads.
    - The filename convention, in the form gsc, kind, dimension, start, end, and an optional partial marker with the last final date, and what the partial marker means: Search Console had not finalised data for the whole requested window at export time.
    - A provenance table with one row per committed file: filename, dimension, requested window, true final-data end date, row count, and the exact reproducing command including every flag and value.
    - A section for the dated re-pull: Search Console finalises the 2026-08-30 data on or after 2026-09-02, the exact command to re-pull the complete cohort, the instruction to replace the partial file and update this table, and a pointer to plan 01-05 which closes it. State plainly that the missing days must never be estimated, averaged, extrapolated, or taken from any other source.
    - A line stating that the committed data is aggregate query and page performance for one property and contains no player or guild names.
    Do not use typographic long dash characters anywhere in this file; the project forbids them in authored content. Use commas, colons, or parentheses instead.

    Before committing, confirm no export row and no README line contains an OAuth client id, client secret, or refresh token value.
  </action>
  <verify>
    <automated>ls scripts/analytics/exports/gsc-trend-query-2026-05-24_2026-08-23.csv scripts/analytics/exports/gsc-trend-page-2026-05-24_2026-08-23.csv && test "$(ls scripts/analytics/exports/gsc-baseline-cohort-query-2026-08-24_2026-08-30*.csv | wc -l | tr -d ' ')" = "1"</automated>
    <automated>head -1 scripts/analytics/exports/gsc-trend-query-2026-05-24_2026-08-23.csv | grep -qx 'query,clicks,impressions,ctr,position,cluster' && head -1 scripts/analytics/exports/gsc-trend-page-2026-05-24_2026-08-23.csv | grep -qx 'page,clicks,impressions,ctr,position'</automated>
    <automated>python3 -c "
import csv, glob, sys
A = {'brand', 'competitor', 'problem', 'expansion', 'unclustered'}
paths = sorted(glob.glob('scripts/analytics/exports/gsc-*-query-*.csv'))
assert len(paths) == 2, paths
for p in paths:
    rows = list(csv.DictReader(open(p, newline='')))
    assert rows, 'no data rows in ' + p
    bad = sorted({r['cluster'] for r in rows} - A)
    assert not bad, (p, bad)
    counts = {}
    for r in rows:
        counts[r['cluster']] = counts.get(r['cluster'], 0) + 1
    assert sum(counts.values()) == len(rows), (p, counts, len(rows))
    print(p, len(rows), counts)
"</automated>
    <automated>test "$(LC_ALL=C grep -c $'\xe2\x80\x94' scripts/analytics/exports/README.md || true)" = "0" && grep -Fq '2026-09-02' scripts/analytics/exports/README.md && grep -Fq 'pull-gsc.py --start' scripts/analytics/exports/README.md</automated>
    <automated>test -z "$(git check-ignore scripts/analytics/exports/README.md || true)" && test -z "$(git status --porcelain --ignored scripts/analytics/exports | grep '^!!' || true)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/analytics/exports/gsc-trend-query-2026-05-24_2026-08-23.csv` exists, its first line is exactly `query,clicks,impressions,ctr,position,cluster`, and it has at least one data row
    - `scripts/analytics/exports/gsc-trend-page-2026-05-24_2026-08-23.csv` exists and its first line is exactly `page,clicks,impressions,ctr,position`
    - Exactly one file matches `scripts/analytics/exports/gsc-baseline-cohort-query-2026-08-24_2026-08-30*.csv`
    - When the run date is earlier than 2026-09-02, that cohort filename contains `-PARTIAL-through-` followed by an ISO date, and that date equals the value the script reported as the last final-data date
    - For both query-dimension exports, every `cluster` value is in the closed five-value set and the bucket counts sum to the row count
    - `scripts/analytics/exports/README.md` exists, is at least 30 lines, and contains a provenance row for each of the three committed files with its requested window, true final-data end date, row count, and reproducing command
    - `scripts/analytics/exports/README.md` contains `2026-09-02` and the literal `pull-gsc.py --start`
    - `scripts/analytics/exports/README.md` contains zero occurrences of the typographic long dash code point, checked byte-wise so the check is not self-invalidating
    - `git check-ignore scripts/analytics/exports/README.md` produces no output, and nothing under `scripts/analytics/exports` is reported as ignored
    - `grep -rniE "GOCSPX|client_secret|refresh_token" scripts/analytics/exports/` returns no line carrying a credential value
  </acceptance_criteria>
  <done>Three real, reproducible exports are committed, the cohort export states its true coverage in its own filename, and the README lets a skeptic re-run every one of them.</done>
</task>

<task type="auto">
  <name>Task 2: Record the dated cohort re-pull as an open item in STATE.md</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (the file being modified; read the Blockers and Concerns section to match its existing bullet style, including the strikethrough-plus-RESOLVED convention used for the OAuth item)
    - scripts/analytics/exports/README.md (written in task 1; the STATE.md entry must state the same true final-data end date and the same command, with no drift between them)
    - .planning/phases/01-measurement-baseline-ai-answer-log/01-CONTEXT.md (D-03, which requires an explicit dated follow-up)
  </read_first>
  <action>
    Add one entry to the Blockers and Concerns list in `.planning/STATE.md`, matching the existing bullet format. It must state, concretely: that the Aug 24 to 30 cohort export is partial; the true last date of final Search Console data at export time, taken from the README rather than assumed; that 2026-09-02 is the earliest date the complete window can be pulled; the exact reproducing command with its flags and values; that plan 01-05 closes it; and that the missing days must never be estimated or sourced from anywhere else (D-03).

    Also update the Session Continuity block's "Last session" and "Stopped at" lines to reflect Phase 1 execution rather than roadmap creation. Leave the Performance Metrics and Progress sections alone; those are maintained by the execute-phase orchestrator.

    Do not use typographic long dash characters in the text you add.
  </action>
  <verify>
    <automated>grep -Fq '2026-09-02' .planning/STATE.md && grep -Fq '01-05' .planning/STATE.md && grep -Fq 'pull-gsc.py --start 2026-08-24 --end 2026-08-30' .planning/STATE.md</automated>
    <automated>python3 -c "
import re
s = open('.planning/STATE.md').read()
block = s.split('### Blockers/Concerns')[1].split('## Deferred Items')[0]
assert 'PARTIAL' in block or 'partial' in block, 'no partial cohort entry in Blockers/Concerns'
assert '2026-09-02' in block, 'earliest re-pull date not in Blockers/Concerns'
assert '01-05' in block, 'closing plan not named in Blockers/Concerns'
m = re.search(r'through[- ](2026-08-2[0-9])', block)
assert m, 'true final-data end date not stated in Blockers/Concerns'
print('ok', m.group(1))
"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` Blockers and Concerns section contains a new entry naming the partial cohort export
    - That entry contains the true final-data end date as an ISO date, matching the date recorded in `scripts/analytics/exports/README.md`
    - That entry contains `2026-09-02` as the earliest valid re-pull date
    - That entry contains the literal command `pull-gsc.py --start 2026-08-24 --end 2026-08-30`
    - That entry names plan `01-05` as the closer
    - That entry states that the missing days must not be estimated or taken from another source
    - The Session Continuity block's Last session and Stopped at lines describe Phase 1 execution
    - The new text contains zero typographic long dash characters
  </acceptance_criteria>
  <done>The partial cohort export cannot be silently forgotten: STATE.md carries the date, the command, the true coverage, and the plan that closes it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env.local` to script process | OAuth client secret and refresh token cross into the process on every pull |
| Search Console API response to committed file | Untrusted remote JSON becomes a permanent repository artifact |
| committed exports to public GitHub | Everything written under `scripts/analytics/exports/` becomes publicly readable |
| exports README and STATE.md | Two records of the same coverage fact; drift between them is how a partial export gets mistaken for a complete one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | credential handling during the three pulls | high | mitigate | The pull path never prints or writes secret values; an explicit pre-commit grep over `scripts/analytics/exports/` for credential-shaped strings is an acceptance criterion of task 1. `.gitignore` already excludes `.env*` and `.env*.local`. |
| T-01-04 | Tampering | the cohort export's coverage claim | high | mitigate | Coverage is discovered from a live date-dimension query, not assumed from the documented lag, and is written into the filename by `partial_suffix_path` rather than typed by hand. The same date is asserted present in both the README and STATE.md, so the two records cannot drift. No task may estimate, average, or substitute a missing day. |
| T-01-03 | Information Disclosure | aggregate query and page performance data in a public repository | medium | accept | The repository is confirmed public and the project already treats it as a transparency signal, and EVID-02 requires committed reproducible queries. The data is aggregate performance for one property with no player or guild names, so this is competitive-intelligence exposure rather than a privacy exposure. Recorded as RESEARCH.md Assumption A4. |
| T-01-11 | Tampering | a hand edited CSV after export | medium | mitigate | Task 1 forbids post-export hand editing, and the cluster-integrity check re-reads every committed query export through `csv.DictReader`, so a broken column or an out-of-set cluster value fails the gate. |
| T-01-SC | Tampering | npm / pip / cargo installs | high | accept | No package-manager install occurs. Stdlib-only Python per repo convention; RESEARCH.md's Package Legitimacy Audit records the gate as not applicable, so there is no `[ASSUMED]` or `[SUS]` package to verify. |
</threat_model>

<verification>
- Three export files exist under `scripts/analytics/exports/`, exactly one of them matching the cohort glob
- Both query-dimension exports pass the closed-set and bucket-sum cluster integrity check
- The cohort filename's partial marker date equals the date recorded in the README and in STATE.md
- `scripts/analytics/exports/README.md` carries a reproducing command per file and the 2026-09-02 re-pull date
- Nothing under `scripts/analytics/exports/` is git-ignored
- No credential-shaped string appears anywhere under `scripts/analytics/exports/`
- Byte-wise long-dash check on `scripts/analytics/exports/README.md` returns zero
</verification>

<success_criteria>
- Success criterion 1 is satisfied for the prior three months and satisfied-with-a-labelled-gap for the cohort, exactly as D-03 directs
- Every published cluster figure traces to a committed file and a committed command
- The remaining gap is explicit, dated, assigned to plan 01-05, and impossible to mistake for complete data
</success_criteria>

<output>
Create `.planning/phases/01-measurement-baseline-ai-answer-log/01-03-SUMMARY.md` when done
</output>
